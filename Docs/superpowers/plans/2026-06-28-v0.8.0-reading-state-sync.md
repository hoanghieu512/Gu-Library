# v0.8.0 — Dedupe "Chưa phân loại" + reading-state đa file & sync — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans. Steps use checkbox (`- [ ]`).
> **Version:** repo đang **0.6.3**; beat này có tính năng mới → bump thẳng **0.8.0** (theo prompt).

**Goal:** (1) Home chỉ còn một "Chưa phân loại" (folder thật, cuối danh sách, icon `?`); (2) nhớ nhiều tài liệu đọc dở, mỗi file một tiến độ, **sync đa máy** qua per-device file `_reading-<deviceId>.json` ở root kho (Option D, conflict = 0); (3) UX: card lớn (mới nhất) + sheet liệt kê đầy đủ + vuốt xoá entry; (4) Home tự re-scan khi về foreground.

**Architecture:** Reading-state chuyển từ Preferences cục bộ → **nhiều file JSON per-device ở root kho**, mỗi máy chỉ ghi file của mình; app render = **union** mọi `_reading-*.json` (mỗi path lấy action mới nhất: đọc vs xoá, qua tombstone). Logic union/merge + suy đường-dẫn là **thuần, TDD**; IO qua plugin `Saf` đã có (`writeFile`/`readFile`/`listFolder`). Doc key device-independent = **đường dẫn tương đối trong kho** (vì SAF content-URI khác nhau giữa máy).

**Tech Stack:** Capacitor 8 (`Saf` plugin, `@capacitor/app` resume, `@capacitor/preferences`), Ionic React (IonModal sheet + IonItemSliding), `crypto.randomUUID()`, Vitest.

**Phạm vi:** CHỈ v0.8.0 (4 phần trên). KHÔNG đụng worker/M7, search/M-Phase2, M10 (xoá/đổi tên/chuyển môn). Reading-state KHÔNG ghi vào sidecar (spec 14.4). KHÔNG file tổng tập trung.

**Tiền đề:** M1–M6 + multi-file (v0.6.3) đã xong, chạy trên 2 dev phone. `Saf.writeFile({dirUri,name,content})` ghi text (overwrite: xoá cũ rồi tạo) — dùng ghi JSON vào root. `Saf.readFile` trả text. `getRootUri()` trả tree-URI root.

---

## Khái niệm dữ liệu chốt (dùng xuyên các task)

```ts
// Một tài liệu đang đọc dở. KEY = path (đường dẫn tương đối trong kho, độc lập máy).
interface ReadingEntry {
  path: string;       // vd "Luật Công chứng/DAC_Yellow_Paper_v1.3.pdf" (gồm tên file pdf)
  name: string;       // hiển thị = basename bỏ đuôi
  monName: string;    // segment đầu của path
  page: number;
  total: number;
  lastReadAt: number; // monotonic ms
}

// Mỗi máy MỘT file _reading-<deviceId>.json ở root kho.
interface DeviceReadingFile {
  deviceId: string;
  entries: Record<string, ReadingEntry>; // path -> entry
  tombstones: Record<string, number>;    // path -> removedAt (đã xoá/đọc-xong lúc nào)
}
```

**Quy tắc union (mọi máy):** với mỗi `path`, so `lastReadAt` lớn nhất (từ mọi `entries`) với `removedAt` lớn nhất (từ mọi `tombstones`). Action mới hơn thắng: tombstone mới hơn → **ẩn**; ngược lại hiện entry có `lastReadAt` lớn nhất. (Đọc lại file đã xoá → `lastReadAt` mới > tombstone → hiện lại.)

**Quy tắc entry:** mở xem (kể cả trang 1) → upsert entry + xoá tombstone cùng path. Đọc tới trang cuối (`page >= total`) → xoá entry + đặt tombstone (tự rời). Vuốt xoá → xoá entry + đặt tombstone.

---

## Phase 1 — Dedupe "Chưa phân loại" (folder thật, cuối, icon `?`)

### Task 1.1: listMon đẩy "Chưa phân loại" xuống cuối (TDD logic sort)

**Files:** Create `src/storage/sortMon.ts` + `sortMon.test.ts`; Modify `src/storage/repo.ts`.

- [ ] **Step 1: Test thất bại trước** — `src/storage/sortMon.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { sortMons } from './sortMon';
import type { Mon } from './types';
const m = (name: string, order?: number): Mon => ({ name, uri: 'u:' + name, meta: order === undefined ? {} : { order } });

describe('sortMons', () => {
  it('order tăng dần trước, rồi alphabet vi', () => {
    expect(sortMons([m('B', 2), m('A', 1), m('Zỹ')]).map((x) => x.name)).toEqual(['A', 'B', 'Zỹ']);
  });
  it('"Chưa phân loại" LUÔN cuối, bất kể order/alphabet', () => {
    const r = sortMons([m('Chưa phân loại', 1), m('Tố tụng', 5), m('An')]);
    expect(r[r.length - 1].name).toBe('Chưa phân loại');
  });
});
```
- [ ] **Step 2: FAIL** — `npx vitest run src/storage/sortMon.test.ts`.
- [ ] **Step 3: Implement `src/storage/sortMon.ts`:**
```ts
import type { Mon } from './types';
import { UNFILED } from '../import/prefix';

export function sortMons(mons: Mon[]): Mon[] {
  return [...mons].sort((a, b) => {
    if (a.name === UNFILED) return 1;   // luôn cuối
    if (b.name === UNFILED) return -1;
    const ao = a.meta.order, bo = b.meta.order;
    if (ao !== undefined && bo !== undefined && ao !== bo) return ao - bo;
    if (ao !== undefined && bo === undefined) return -1;
    if (ao === undefined && bo !== undefined) return 1;
    return a.name.localeCompare(b.name, 'vi');
  });
}
```
- [ ] **Step 4: PASS.** Then in `repo.ts` `listMon`: replace the inline `mons.sort(...)` block with `return sortMons(mons);` (import `sortMons`). "Chưa phân loại" KHÔNG còn bị loại — nó là folder thật, vào danh sách như môn, chỉ sắp cuối.
- [ ] **Step 5:** `npm test && npm run build` PASS. Commit `feat(v0.8.0): sort 'Chưa phân loại' folder last in mon list`.

### Task 1.2: MonCard render "Chưa phân loại" như system-folder (icon `?`, trầm)

**Files:** Modify `src/components/MonCard.tsx`.

- [ ] **Step 1:** Trong MonCard, khi `mon.name === UNFILED` (import `UNFILED` từ `../import/prefix`): render `UnfiledSwatch` (ô `?` viền đứt xám) thay `MonSwatch`, và label dùng kiểu trầm (italic, `color: var(--gu-grey)`, không serif đậm) thay tiêu đề serif nâu. Badge "N chờ" giữ nguyên (vẫn cộng `inboxPending`). Các môn thật không đổi.
- [ ] **Step 2:** `npm run build` PASS. Commit `feat(v0.8.0): render unclassified folder as muted system card (?, grey)`.

### Task 1.3: Gỡ mục ảo "Chưa phân loại" khỏi HomePage

**Files:** Modify `src/pages/HomePage.tsx`.

- [ ] **Step 1:** Xoá block `{(inboxMap.get(UNFILED) ?? 0) > 0 && (...)}` (mục ảo). Gỡ import giờ thừa nếu không nơi nào khác dùng (`UnfiledSwatch`, `hourglassOutline`, `IonIcon`, `UNFILED`) — kiểm bằng grep, chỉ gỡ cái thật sự thừa. `inboxMap` GIỮ (vẫn truyền `inboxPending` cho MonCard; folder "Chưa phân loại" nếu tồn tại sẽ tự cộng `inboxMap.get(UNFILED)`).
- [ ] **Step 2:** `npm test && npm run build` PASS (smoke "Gú's Library" còn xanh). Commit `feat(v0.8.0): drop virtual unclassified row (real folder handles it)`.

> Kết quả Phase 1: "Chưa phân loại" chỉ hiện khi **folder thật tồn tại**, luôn cuối, icon `?`, trầm. Chưa có folder → không hiện gì.

---

## Phase 2 — Reading model thuần (TDD)

### Task 2.1: Merge/union + types

**Files:** Create `src/reading/model.ts` + `model.test.ts`.

- [ ] **Step 1: Test thất bại trước** — `src/reading/model.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { mergeReading } from './model';
import type { DeviceReadingFile, ReadingEntry } from './model';

const E = (path: string, page: number, at: number): ReadingEntry =>
  ({ path, name: path.split('/').pop()!.replace(/\.[^.]+$/, ''), monName: path.split('/')[0], page, total: 100, lastReadAt: at });
const F = (deviceId: string, entries: ReadingEntry[], tomb: Record<string, number> = {}): DeviceReadingFile =>
  ({ deviceId, entries: Object.fromEntries(entries.map((e) => [e.path, e])), tombstones: tomb });

describe('mergeReading', () => {
  it('union nhiều máy; mỗi path 1 lần; lấy lastReadAt mới nhất', () => {
    const r = mergeReading([F('d1', [E('m/a.pdf', 5, 10)]), F('d2', [E('m/a.pdf', 9, 20), E('m/b.pdf', 2, 15)])]);
    expect(r.map((x) => [x.path, x.page])).toEqual([['m/a.pdf', 9], ['m/b.pdf', 2]]); // sort desc theo at: a(20) > b(15)
  });
  it('tombstone mới hơn entry → ẩn', () => {
    const r = mergeReading([F('d1', [E('m/a.pdf', 5, 10)]), F('d2', [], { 'm/a.pdf': 30 })]);
    expect(r).toHaveLength(0);
  });
  it('đọc lại sau khi xoá (entry mới hơn tombstone) → hiện lại', () => {
    const r = mergeReading([F('d1', [E('m/a.pdf', 5, 40)]), F('d2', [], { 'm/a.pdf': 30 })]);
    expect(r).toHaveLength(1);
    expect(r[0].page).toBe(5);
  });
  it('sort lastReadAt giảm dần', () => {
    const r = mergeReading([F('d1', [E('m/a.pdf', 1, 10), E('m/c.pdf', 1, 99), E('m/b.pdf', 1, 50)])]);
    expect(r.map((x) => x.path)).toEqual(['m/c.pdf', 'm/b.pdf', 'm/a.pdf']);
  });
});
```
- [ ] **Step 2: FAIL** — `npx vitest run src/reading/model.test.ts`.
- [ ] **Step 3: Implement `src/reading/model.ts`:**
```ts
export interface ReadingEntry {
  path: string; name: string; monName: string; page: number; total: number; lastReadAt: number;
}
export interface DeviceReadingFile {
  deviceId: string;
  entries: Record<string, ReadingEntry>;
  tombstones: Record<string, number>;
}

// Union mọi file device → danh sách entry hiển thị (sort lastReadAt giảm dần).
// Mỗi path: action mới nhất thắng (đọc vs xoá). KHÔNG lọc tồn tại file (việc của repo).
export function mergeReading(files: DeviceReadingFile[]): ReadingEntry[] {
  const bestEntry = new Map<string, ReadingEntry>();
  const bestTomb = new Map<string, number>();
  for (const f of files) {
    for (const e of Object.values(f.entries ?? {})) {
      const cur = bestEntry.get(e.path);
      if (!cur || e.lastReadAt > cur.lastReadAt) bestEntry.set(e.path, e);
    }
    for (const [p, t] of Object.entries(f.tombstones ?? {})) {
      const cur = bestTomb.get(p);
      if (cur === undefined || t > cur) bestTomb.set(p, t);
    }
  }
  const out: ReadingEntry[] = [];
  for (const [path, e] of bestEntry) {
    const tomb = bestTomb.get(path);
    if (tomb !== undefined && tomb >= e.lastReadAt) continue; // đã xoá sau lần đọc
    out.push(e);
  }
  return out.sort((a, b) => b.lastReadAt - a.lastReadAt);
}

// upsert/remove thuần trên 1 file device (repo lo IO + nowMs).
export function upsertEntry(file: DeviceReadingFile, e: ReadingEntry): DeviceReadingFile {
  const entries = { ...file.entries, [e.path]: e };
  const tombstones = { ...file.tombstones };
  delete tombstones[e.path]; // đọc lại → bỏ tombstone
  return { ...file, entries, tombstones };
}
export function removeEntry(file: DeviceReadingFile, path: string, at: number): DeviceReadingFile {
  const entries = { ...file.entries };
  delete entries[path];
  return { ...file, entries, tombstones: { ...file.tombstones, [path]: at } };
}
```
- [ ] **Step 4: PASS** (4 tests). Thêm test cho `upsertEntry`/`removeEntry` (bỏ tombstone khi upsert; set tombstone khi remove). Commit `feat(v0.8.0): reading merge model (per-device union + tombstones) with tests`.

### Task 2.2: relPath helpers (TDD phần thuần)

**Files:** Create `src/reading/paths.ts` + `paths.test.ts`.

- [ ] **Step 1: Test** `paths.test.ts` cho `relPathFromUris(rootUri, docUri)`:
```ts
import { describe, it, expect } from 'vitest';
import { relPathFromUris } from './paths';
const ROOT = 'content://com.android.externalstorage.documents/tree/primary%3ADownload%2Fkho';
const DOC = ROOT + '/document/primary%3ADownload%2Fkho%2FLu%E1%BA%ADt%20C%C3%B4ng%20ch%E1%BB%A9ng%2FDAC.pdf';
describe('relPathFromUris', () => {
  it('trả đường dẫn tương đối đầy đủ trong kho', () => {
    expect(relPathFromUris(ROOT, DOC)).toBe('Luật Công chứng/DAC.pdf');
  });
  it('doc ngoài kho → null', () => {
    expect(relPathFromUris(ROOT, 'content://x/tree/other/document/other%2Ffile.pdf')).toBeNull();
  });
});
```
- [ ] **Step 2: FAIL → Implement `src/reading/paths.ts`** (tách `treeId`/`documentId` như `monPath.ts`, trả `rel` đầy đủ thay vì segment đầu):
```ts
function treeId(uri: string): string | null { const m = uri.match(/\/tree\/([^/]+)/); return m ? decodeURIComponent(m[1]) : null; }
function documentId(uri: string): string | null { const m = uri.match(/\/document\/([^/]+)/); return m ? decodeURIComponent(m[1]) : null; }

export function relPathFromUris(rootUri: string, docUri: string): string | null {
  const root = treeId(rootUri);
  const doc = documentId(docUri) ?? treeId(docUri);
  if (!root || !doc) return null;
  const prefix = root.endsWith('/') ? root : root + '/';
  if (!doc.startsWith(prefix)) return null;
  return doc.slice(prefix.length);
}
```
- [ ] **Step 3: PASS.** Commit `feat(v0.8.0): relPathFromUris (device-independent doc key)`.

> `resolveUriFromRelPath` (đi từ path → SAF uri trên máy hiện tại) cần `Saf.listFolder` thật → đặt ở repo (Task 3), không test jsdom.

---

## Phase 3 — Reading repository (SAF-backed)

### Task 3.1: deviceId + IO file device + API

**Files:** Create `src/reading/store.ts`. (Sẽ thay `src/reading/progress.ts` ở Task 4.)

- [ ] **Step 1: Viết `src/reading/store.ts`:**
```ts
import { Preferences } from '@capacitor/preferences';
import { Saf } from '../plugins/saf';
import { getRootUri } from '../storage/repo';
import { relPathFromUris } from './paths';
import { mergeReading, upsertEntry, removeEntry as removeInFile,
         type DeviceReadingFile, type ReadingEntry } from './model';

const DEVICE_KEY = 'device_id';
let _seq = 0;
function nowMs(): number { return Date.now() * 1000 + (_seq++ % 1000); }

export async function getDeviceId(): Promise<string> {
  const { value } = await Preferences.get({ key: DEVICE_KEY });
  if (value) return value;
  const id = crypto.randomUUID();
  await Preferences.set({ key: DEVICE_KEY, value: id });
  return id;
}

const fileName = (deviceId: string) => `_reading-${deviceId}.json`;
const isReadingFile = (name: string) => name.startsWith('_reading-') && name.endsWith('.json');

async function readDeviceFile(root: string, deviceId: string): Promise<DeviceReadingFile> {
  const { entries } = await Saf.listFolder({ uri: root });
  const f = entries.find((e) => !e.isDirectory && e.name === fileName(deviceId));
  if (f) {
    try {
      const { data } = await Saf.readFile({ uri: f.uri });
      const parsed = JSON.parse(data) as DeviceReadingFile;
      return { deviceId, entries: parsed.entries ?? {}, tombstones: parsed.tombstones ?? {} };
    } catch { /* hỏng → tạo mới */ }
  }
  return { deviceId, entries: {}, tombstones: {} };
}

async function writeDeviceFile(root: string, file: DeviceReadingFile): Promise<void> {
  await Saf.writeFile({ dirUri: root, name: fileName(file.deviceId), content: JSON.stringify(file) });
}

async function readAllFiles(root: string): Promise<DeviceReadingFile[]> {
  const { entries } = await Saf.listFolder({ uri: root });
  const out: DeviceReadingFile[] = [];
  for (const e of entries) {
    if (e.isDirectory || !isReadingFile(e.name)) continue;
    try {
      const { data } = await Saf.readFile({ uri: e.uri });
      const p = JSON.parse(data) as DeviceReadingFile;
      out.push({ deviceId: p.deviceId ?? e.name, entries: p.entries ?? {}, tombstones: p.tombstones ?? {} });
    } catch { /* bỏ file hỏng */ }
  }
  return out;
}

function baseName(relPath: string): string { return relPath.split('/').pop()!.replace(/\.[^.]+$/, ''); }

// Ghi tiến độ (gọi từ Viewer). page>=total → coi như xong: xoá entry + tombstone.
export async function recordProgress(docUri: string, page: number, total: number): Promise<void> {
  const root = await getRootUri(); if (!root) return;
  const path = relPathFromUris(root, docUri); if (!path) return;
  const deviceId = await getDeviceId();
  let file = await readDeviceFile(root, deviceId);
  if (total > 0 && page >= total) {
    file = removeInFile(file, path, nowMs());
  } else {
    const entry: ReadingEntry = { path, name: baseName(path), monName: path.split('/')[0], page, total, lastReadAt: nowMs() };
    file = upsertEntry(file, entry);
  }
  await writeDeviceFile(root, file);
}

// Vuốt xoá khỏi danh sách (chỉ entry; file trong kho giữ nguyên).
export async function removeReading(path: string): Promise<void> {
  const root = await getRootUri(); if (!root) return;
  const deviceId = await getDeviceId();
  const file = await readDeviceFile(root, deviceId);
  await writeDeviceFile(root, removeInFile(file, path, nowMs()));
}

// Trang để khôi phục khi mở (union mọi máy, mới nhất theo path).
export async function getResumePage(docUri: string): Promise<number> {
  const root = await getRootUri(); if (!root) return 1;
  const path = relPathFromUris(root, docUri); if (!path) return 1;
  const merged = mergeReading(await readAllFiles(root));
  return merged.find((e) => e.path === path)?.page ?? 1;
}

// Danh sách đang đọc dở (union) + resolve uri trên máy này; ẩn entry không còn file.
export interface ReadingItem extends ReadingEntry { uri: string; }
export async function listReading(): Promise<ReadingItem[]> {
  const root = await getRootUri(); if (!root) return [];
  const merged = mergeReading(await readAllFiles(root));
  const out: ReadingItem[] = [];
  for (const e of merged) {
    const uri = await resolveUriFromRelPath(root, e.path);
    if (uri) out.push({ ...e, uri });
  }
  return out;
}

// path (vd "Môn/Chương/x.pdf") → SAF document uri trên máy hiện tại; null nếu không còn.
async function resolveUriFromRelPath(root: string, path: string): Promise<string | null> {
  const segs = path.split('/').filter(Boolean);
  let curUri = root;
  for (let i = 0; i < segs.length; i++) {
    const { entries } = await Saf.listFolder({ uri: curUri });
    const want = segs[i];
    const isLast = i === segs.length - 1;
    const hit = entries.find((e) => e.name === want && (isLast ? !e.isDirectory : e.isDirectory));
    if (!hit) return null;
    curUri = hit.uri;
  }
  return curUri;
}
```
- [ ] **Step 2:** `npm run build` PASS. Commit `feat(v0.8.0): per-device reading store (SAF files, union, resolve, record/remove)`.

---

## Phase 4 — Wire Viewer + Home + classify + migration

### Task 4.1: classify bỏ qua mọi file `_`-prefix

**Files:** Modify `src/storage/classify.ts`.
- [ ] **Step 1:** Đổi check `if (name === MON_JSON) continue;` thành `if (name.startsWith('_')) continue;` (bao luôn `_mon.json` + `_reading-*.json` + `_*` tương lai). Giữ skip dot-file. Thêm 1 test trong `classify.test.ts`: file `_reading-abc.json` bị bỏ (không thành document/pending).
- [ ] **Step 2:** `npm test && npm run build` PASS. Commit `feat(v0.8.0): classify ignores all _-prefixed files`.

### Task 4.2: Viewer dùng store mới

**Files:** Modify `src/pages/ViewerPage.tsx`; (giữ `monPath` cho tiêu đề nếu cần).
- [ ] **Step 1:** Thay `getProgressFor(docUri)` → `getResumePage(docUri)` (trả số trang) cho `initialPage`. Thay `setProgress({docUri,name,monName,page,total})` trong `onPageChange` → `recordProgress(docUri, page, total)` (store tự suy path/name/mon). Bỏ import `setProgress/getProgressFor` từ `progress.ts`, import từ `../reading/store`.
- [ ] **Step 2:** `npm test && npm run build` PASS. Commit `feat(v0.8.0): viewer records/resumes via per-device reading store`.

### Task 4.3: ContinueReadingCard + HomePage dùng listReading + migration

**Files:** Modify `src/components/ContinueReadingCard.tsx`, `src/pages/HomePage.tsx`; Create `src/reading/migrate.ts`.
- [ ] **Step 1: migrate (one-time):** `src/reading/migrate.ts` — nếu Preferences `reading_progress` còn + root có quyền: với mỗi Progress cũ, `relPathFromUris(root, p.docUri)` → nếu ra path, `recordProgress`-style seed vào device file (giữ `lastReadAt` cũ); xong xoá key `reading_progress`. Lỗi/không suy được → bỏ qua (chấp nhận mất, theo prompt). Gọi `migrateOnce()` ở đầu HomePage reload (idempotent: key đã xoá thì no-op).
- [ ] **Step 2: HomePage:** thêm state `reading: ReadingItem[]`; trong `reload()` gọi `setReading(await listReading())` (thay `getContinueReading`). `cont` = `reading[0] ?? null`. Truyền cho ContinueReadingCard kiểu `ReadingItem` (có `uri` để mở thẳng). Khu "Đang đọc dở" bọc trong vùng tap mở sheet (Task 5).
- [ ] **Step 3: ContinueReadingCard:** nhận `ReadingItem` (đã có `uri`, `name`, `monName`, `page`, `total`); tap → `history.push('/viewer/' + encodeUriParam(uri))` (bỏ derive monName cũ — đã có sẵn). 
- [ ] **Step 4:** `npm test && npm run build` PASS. Commit `feat(v0.8.0): Home continue-reading from union list + one-time migration`.

> Sau Task 4: `src/reading/progress.ts` cũ hết người dùng → xoá ở Task 5 (sau khi sheet cũng chuyển xong) hoặc ngay nếu không còn import (`grep`).

---

## Phase 5 — Sheet "Đang đọc dở" + vuốt xoá

### Task 5.1: ReadingListSheet

**Files:** Create `src/reading/ReadingListSheet.tsx`; Modify `src/pages/HomePage.tsx`.
- [ ] **Step 1:** `ReadingListSheet` (IonModal breakpoints `[0,0.6,0.95]`): IonList các `ReadingItem` (đã sort desc), mỗi dòng `IonItemSliding` → `IonItem` (tên serif + môn + "Trang X / Y") + `IonItemOptions` (vuốt) nút "Bỏ" gọi `onRemove(path)`. Tap item → `onOpen(uri)`. Props: `{ isOpen, items, onOpen, onRemove, onClose }`.
- [ ] **Step 2: HomePage:** state `sheetOpen`. Vùng "Đang đọc dở" (card lớn + tiêu đề) bọc `onClick={() => setSheetOpen(true)}` — NHƯNG card lớn tap phải MỞ TÀI LIỆU (0 chạm phụ), nên: card giữ onClick mở viewer (stopPropagation), còn tiêu đề/khu xung quanh mở sheet. Render `<ReadingListSheet isOpen={sheetOpen} items={reading} onOpen={(uri)=>{setSheetOpen(false); history.push('/viewer/'+encodeUriParam(uri));}} onRemove={async(p)=>{await removeReading(p); reload();}} onClose={()=>setSheetOpen(false)} />`.
- [ ] **Step 3:** `npm test && npm run build` PASS. Xoá `src/reading/progress.ts` nếu hết import (`grep -rn "reading/progress" src/`). Commit `feat(v0.8.0): reading list sheet (full list, swipe to remove)`.

---

## Phase 6 — Refresh khi về foreground

### Task 6.1: HomePage re-scan on resume

**Files:** Modify `src/pages/HomePage.tsx`.
- [ ] **Step 1:** Trong `useEffect` mount, thêm `@capacitor/app` resume listener gọi `reload()` (cùng kiểu ShareReceiver: `const sub = App.addListener('resume', () => reload()); return () => { sub.then(h=>h.remove()); off(); }`). Giữ `onKhoChanged` + `useIonViewWillEnter`. → badge ⏳, số tài liệu, danh sách đọc dở (sync từ máy khác) đều cập nhật khi quay lại app.
- [ ] **Step 2:** `npm test && npm run build` PASS. Commit `feat(v0.8.0): Home re-scans kho on app resume/foreground`.

---

## Phase 7 — Build + nghiệm thu máy thật + 0.8.0

### Task 7.1: Build + install (cả 2 phone nếu có)
- [ ] `bash -lc '... npm run build && npx cap sync android && cd android && ./gradlew assembleDebug'` → cài lên `R5CT844VRCN` (và phone kia nếu cắm) bằng `adb -s <serial> install -r ...`.

### Task 7.2: NGHIỆM THU (đối chiếu testing checklist của prompt)
- [ ] "Chưa phân loại": 1 dòng (folder thật), icon `?`, cuối; chưa có folder → không hiện.
- [ ] Đọc A→tr15, B→tr5, C→tr10: cả 3 còn trong đọc dở, đúng tiến độ.
- [ ] Card lớn = mới nhất; tap = đúng trang.
- [ ] Tap khu "Đang đọc dở" → sheet đầy đủ, sort mới→cũ; tap dòng = đúng trang.
- [ ] Vuốt 1 dòng → biến mất; mở lại từ Môn học vẫn vào được, tiến độ còn.
- [ ] Đọc tới trang cuối → tự rời.
- [ ] **Sync 2 máy:** đọc X tr15 ở phone1 → phone2 (sau sync + foreground) hiện X, mở đúng tr15.
- [ ] **Không conflict:** đọc cùng X ở 2 phone → kho không sinh `.sync-conflict-*` cho `_reading-*.json`. (CC verify: `adb shell ls /sdcard/Download/kho/ | grep -i conflict` → trống; có 2 file `_reading-*.json`.)
- [ ] Foreground → cập nhật không cần tắt-mở.
- [ ] `_reading-*.json` không lọt vào danh sách tài liệu môn.

### Task 7.3: Đóng 0.8.0
- [ ] `package.json` → `0.8.0`; CHANGELOG mục `## [0.8.0]` (dedupe Chưa phân loại + reading-state per-device sync + sheet + foreground refresh). Commit `feat(v0.8.0): reading-state sync + unclassified dedupe — verified on device`.

---

## Self-review notes
- **Phần 1:** folder thật cuối (sortMons) + MonCard system render (icon ?) + gỡ mục ảo + nhận diện bằng tên `Chưa phân loại` (UNFILED) — không cần flag `_mon.json`. Sheet import M6 không đụng. ✓
- **Phần 2:** per-device file `_reading-<id>.json` ở root, mỗi máy chỉ ghi file mình (writeDeviceFile dùng deviceId của máy) → conflict 0. Union qua `mergeReading` (tombstone = last-action-wins, hỗ trợ vuốt-xoá xuyên máy + đọc-lại-hiện-lại). Key = relPath (độc lập máy). Entry trỏ doc đã mất → `resolveUriFromRelPath` trả null → ẩn. Không ghi sidecar, không file tổng. ✓
- **Phần 3:** card lớn = `reading[0]` (mới nhất), tap mở thẳng; sheet full sort desc; vuốt = `removeReading` (tombstone). Vào list khi mở (record tr hiện tại); rời khi `page>=total`; không cap. ✓
- **Phần 4:** HomePage reload (qua `useIonViewWillEnter` + `onKhoChanged` + **resume**) đọc lại `_reading-*.json` (sync từ máy khác) → union cập nhật. ✓
- **classify** bỏ `_`-prefix → `_reading-*.json` không thành tài liệu. ✓
- **TDD:** `mergeReading`/`upsert`/`removeEntry`/`relPathFromUris`/`sortMons` thuần, có test; IO + resolve + UI verify trên thiết bị (2 máy cho sync). ✓
- **Migration:** seed 1 lần từ Preferences cũ nếu suy được path, else bỏ. ✓

## Việc tay (người dùng) khi nghiệm thu
- 2 dev phone cùng trỏ vào kho (Syncthing đã sync) để test sync + no-conflict.
- Đọc/vuốt/foreground theo checklist; CC verify `_reading-*.json` + không `.sync-conflict` qua adb.
