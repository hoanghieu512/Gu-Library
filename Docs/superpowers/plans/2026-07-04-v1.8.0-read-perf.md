# v1.8.0 — Hiệu năng đường đọc (một walk chung + native bulk-query)

**Goal:** Cold start < 5s (từ 17.6s/27.2s), mở môn/sheet không khựng, đếm vòng SAF ~315 → hai chữ số — KHÔNG đổi hành vi nhìn thấy, KHÔNG đụng đường ghi (v1.9.0).

**Hướng (design session 2026-07-04):** một lần tải = MỘT walk toàn kho chia chung mọi consumer; diệt reload đúp; native hỏi một câu cho cả đàn con.

## Tasks
- [x] T1: Native `SafPlugin.listFolder` bulk-query (một cursor DocumentsContract) — bỏ nameLoop per-child; interface giữ nguyên.
- [x] T2: `src/storage/khoSnapshot.ts` — buildSnapshot (một walk) + cache RAM + invalidate theo khoChanged (module) — không ghi file cache Syncthing. Dẫn xuất `foldSummary`/`countFlagged`/`folderByPath`.
- [x] T3: Dẫn 5 hàm hot qua snapshot: `listMon` (repo), `summarizeMon` (summary), `countPrintFlagged` (printRepo), `listInboxByMon` (inboxRepo), `listReading` (store — nội dung _reading đọc tươi, resolve/tên qua cây).
- [x] T4: HomePage — một trigger tải (bỏ reload mount), `resume`→invalidate+reload.
- [x] T5: Unit test dẫn xuất (foldSummary/countFlagged/folderByPath) + suite xanh (108 test).
- [x] T6: Device verify Flip 4 (đo tạm rồi gỡ): cold start 1322ms, 1 walk, 53 SAF, parity 100%, sheet 0 SAF, invalidate đúng.
- [ ] T7: Verify 2 máy QA thật (Flip 4 + Fold 3) — đệ chạy.
- [ ] T8: Bump 1.8.0 (đã) + CHANGELOG + memory + merge main + tag.

Kết quả số: `Docs/perf/2026-07-04-ket-qua-v1.8.0.md`.
