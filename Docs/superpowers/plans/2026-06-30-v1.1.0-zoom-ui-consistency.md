# v1.1.0 — Pinch-zoom Viewer + UI-consistency pass — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development hoặc superpowers:executing-plans. Steps dùng checkbox `- [ ]`.

**Goal:** Đồng nhất ngôn ngữ thị giác toàn app (giải ở tầng token, không vá rời) + thêm pinch-zoom cho Viewer (giữ zoom khi lật trang, không phá nhớ-trang). Bump v1.0.0 → **v1.1.0**.

**Architecture / chẩn đoán gốc:**
- **Xanh dương rò rỉ = lỗi THỨ TỰ CSS, không phải thiếu override.** `src/theme/variables.css` ĐÃ đặt `--ion-color-primary` nâu, nhưng `@ionic/react/css/core.css` (chứa palette mặc định `#3880ff`) được import trong `App.tsx` *sau* `variables.css` (import ở `main.tsx`) → core.css ghi đè lại blue. **Fix: nạp 4 file CSS Ionic TRƯỚC `variables.css`** (chuyển import sang `main.tsx`, bỏ ở `App.tsx`). Sau đó override token của ta thắng → nâu áp đồng loạt cho mọi `IonButton`/`IonBackButton`/icon-action mà KHÔNG phải sửa từng nút.
- **3 token chốt ở `variables.css`:** (1) `--ion-color-primary = #553B08` (nâu đậm, đúng nền card "đọc dở") + `--ion-color-primary-contrast = var(--gu-cream)` (chữ kem cho nút đặc); (2) `--ion-color-success` = một shade duy nhất (theo pill sync) cho mọi badge "xong/thành công"; (3) global `ion-button { text-transform: none; }` → Title case toàn app (bỏ UPPER mặc định md-mode).
- **Pinch-zoom:** giữ nguyên mô hình cuộn dọc nhiều trang. Thêm state `zoom`; mỗi `<Page>` render `width = fitWidth * zoom` (re-raster nét, KHÔNG transform vĩnh viễn nên IntersectionObserver page-tracking + `scrollToPage` resume vẫn nguyên vẹn). Pinch live preview bằng CSS `transform: scale()` (mượt, không re-raster mỗi frame), **commit `setZoom` khi nhấc tay** → re-raster nét. Pan = cuộn native (`overflow:auto` + wrapper `width:max-content;margin:0 auto`). Double-tap → `zoom=1`. `zoom` là state nên **giữ nguyên khi lật trang**.

**Tech Stack:** Ionic React (CSS variables/tokens), react-pdf 10 / pdfjs 5.4.296, native touch events (passive:false để `preventDefault` pinch).

**Bối cảnh đã xác minh:**
- `variables.css` hiện: primary `#75420E`, contrast `#ffffff`; KHÔNG set success; KHÔNG có text-transform.
- Card "đọc dở" (`ContinueReadingCard`) = nền `--gu-brown-deep` (#553B08) + chữ trắng → dùng làm chuẩn nền nút đặc; chữ nút = kem theo brief.
- Sync pill (`SyncPill`) dùng `color="success"`; PrintPage badge cũng `color="success"` → cùng token; chốt 1 shade qua `--ion-color-success`.
- `PdfView` render TẤT CẢ trang trong 1 container cuộn dọc; page-tracking = IntersectionObserver trên `#pdf-page-n`; resume = `scrollToPage(initialPage)`; jump = `scrollToPage(jumpTo)`.
- Labels hiện: ReadingListSheet title `<IonTitle>` serif fontSize 17 (khác heading Home `.gu-title`); nút `Đóng` (render thành "ĐÓNG" do md uppercase). PrintPage: badge "đã gửi đi in", nút "Gom vào _print/ (N)", "Xong", header môn `<h2>` lề trái dính mép.

---

## File Structure

**Modify:**
- `src/main.tsx` — nạp 4 CSS Ionic trước `variables.css`.
- `src/App.tsx` — bỏ 4 import CSS Ionic (đã chuyển sang main).
- `src/theme/variables.css` — primary nâu đậm + contrast kem; success 1 shade; `ion-button` text-transform none.
- `src/reading/ReadingListSheet.tsx` — title `.gu-title`; (nút "Đóng" tự nâu + title case nhờ token).
- `src/pages/PrintPage.tsx` — badge "Đã gửi đi in" title case; nút "Gom để in (N)"; bỏ inline `textTransform`; căn lề header môn.
- `src/components/PdfView.tsx` — pinch-zoom + pan + double-tap.
- `package.json` (1.1.0), `CHANGELOG.md`, `Docs/gu-library-design-spec.md` (quy ước UI), file memory.

---

## Task 1: Token layer — fix CSS load order + 3 token (giết blue + casing app-wide)

**Files:** `src/main.tsx`, `src/App.tsx`, `src/theme/variables.css`

- [ ] **Step 1: Nạp CSS Ionic TRƯỚC variables.css (main.tsx)**

Sửa `src/main.tsx` thành:
```tsx
import React from 'react';
import ReactDOM from 'react-dom/client';
import '@ionic/react/css/core.css';
import '@ionic/react/css/normalize.css';
import '@ionic/react/css/structure.css';
import '@ionic/react/css/typography.css';
import './theme/fonts';
import './theme/variables.css';
import App from './App';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
```

- [ ] **Step 2: Bỏ 4 import CSS Ionic ở App.tsx**

Trong `src/App.tsx`, XÓA 4 dòng:
```tsx
import '@ionic/react/css/core.css';
import '@ionic/react/css/normalize.css';
import '@ionic/react/css/structure.css';
import '@ionic/react/css/typography.css';
```
(Giữ comment `/* Ionic core + theming CSS */` cũng được hoặc xóa; không còn import ở đây vì đã nạp ở main trước variables.css.)

- [ ] **Step 3: variables.css — primary nâu đậm + contrast kem + success 1 shade + button casing**

Trong `src/theme/variables.css`, sửa khối primary thành nâu ĐẬM + chữ kem:
```css
  /* primary = nâu ĐẬM (đúng nền card "đọc dở"); chữ kem cho nút đặc */
  --ion-color-primary: #553B08;
  --ion-color-primary-rgb: 85,59,8;
  --ion-color-primary-contrast: #E9E5CD;
  --ion-color-primary-contrast-rgb: 233,229,205;
  --ion-color-primary-shade: #4a3407;
  --ion-color-primary-tint: #6a4f1f;
```

Thêm khối success (một shade duy nhất, theo pill sync) ngay sau khối primary:
```css
  /* success = MỘT shade xanh lá (pill sync "Đã đồng bộ") cho mọi trạng thái xong */
  --ion-color-success: #2dd36f;
  --ion-color-success-rgb: 45,211,111;
  --ion-color-success-contrast: #ffffff;
  --ion-color-success-contrast-rgb: 255,255,255;
  --ion-color-success-shade: #28ba62;
  --ion-color-success-tint: #42d77d;
```

Thêm cuối file (Title case toàn app — bỏ uppercase mặc định của ion-button md mode):
```css
/* Title case toàn app — không để ion-button tự UPPER (md mode) */
ion-button { text-transform: none; }
```

- [ ] **Step 4: Build + test**

Run: `npm run build && npm test`
Expected: tsc + vite PASS; 76 test PASS.

- [ ] **Step 5: Commit**
```bash
git add src/main.tsx src/App.tsx src/theme/variables.css
git commit -m "fix(v1.1): token layer — nap CSS Ionic truoc variables; primary nau dam + chu kem; success 1 shade; button Title case

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

> Sau task này: "+" tạo môn (Home + FolderPage), "Đóng" sheet, "Xong"/"Gom"/"Nhảy", IonBackButton… đều nâu + Title case TỰ ĐỘNG (không sửa từng cái). Các task sau chỉ lo phần KHÔNG do token bao (label text, font heading, lề).

---

## Task 2: Sheet "Đang đọc dở" — tiêu đề trùng heading Home

**Files:** `src/reading/ReadingListSheet.tsx`

- [ ] **Step 1: Title dùng `.gu-title` (serif + nâu, như Home)**

Trong `src/reading/ReadingListSheet.tsx`, đổi dòng title:
```tsx
          <IonTitle style={{ fontFamily: 'var(--gu-serif)', fontSize: 17 }}>Đang đọc dở</IonTitle>
```
thành:
```tsx
          <IonTitle className="gu-title" style={{ fontSize: 17 }}>Đang đọc dở</IonTitle>
```
(`.gu-title` = serif + 700 + `--gu-brown-deep`, khớp heading Home. Nút "Đóng" đã tự nâu + Title case nhờ token Task 1 — không sửa.)

- [ ] **Step 2: Build**

Run: `npm run build`
Expected: PASS.

- [ ] **Step 3: Commit**
```bash
git add src/reading/ReadingListSheet.tsx
git commit -m "fix(v1.1): tieu de sheet 'Dang doc do' trung font+mau heading Home (.gu-title)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: Màn "Đi in" — label, casing badge, lề header môn

**Files:** `src/pages/PrintPage.tsx`

- [ ] **Step 1: Badge title case + Gom label (bỏ tên kỹ thuật) + bỏ inline textTransform**

Trong `src/pages/PrintPage.tsx`:

(a) Badge: đổi
```tsx
                    <IonBadge slot="end" color="success" style={{ marginRight: 8 }}>đã gửi đi in</IonBadge>
```
thành
```tsx
                    <IonBadge slot="end" color="success" style={{ marginRight: 8 }}>Đã gửi đi in</IonBadge>
```

(b) Nút "Xong": bỏ inline `style={{ textTransform: 'none' }}` (đã global). Đổi
```tsx
                    <IonButton slot="end" size="small" fill="outline" disabled={busy}
                      style={{ textTransform: 'none' }} onClick={() => doDone(r)}>
                      Xong
                    </IonButton>
```
thành
```tsx
                    <IonButton slot="end" size="small" fill="outline" disabled={busy}
                      onClick={() => doDone(r)}>
                      Xong
                    </IonButton>
```

(c) Nút Gom: đổi label (bỏ `_print/`) + bỏ inline textTransform. Đổi
```tsx
              <IonButton expand="block" disabled={busy} style={{ textTransform: 'none' }} onClick={doGom}>
                {busy ? 'Đang gom…' : `Gom vào _print/ (${pendingCopy})`}
              </IonButton>
```
thành
```tsx
              <IonButton expand="block" disabled={busy} onClick={doGom}>
                {busy ? 'Đang gom…' : `Gom để in (${pendingCopy})`}
              </IonButton>
```
(Nút `expand="block"` mặc định primary → nền nâu đậm + chữ kem nhờ token Task 1.)

- [ ] **Step 2: Căn lề header nhóm môn bằng row file**

Đổi header môn:
```tsx
            <h2 className="gu-title" style={{ fontSize: 16, margin: '0 0 4px', color: 'var(--gu-brown)' }}>{mon}</h2>
```
thành (thêm padding-inline-start ngang lề item Ionic mặc định ~16px):
```tsx
            <h2 className="gu-title" style={{ fontSize: 16, margin: '0 0 4px', paddingInlineStart: 16, color: 'var(--gu-brown)' }}>{mon}</h2>
```

- [ ] **Step 3: Build**

Run: `npm run build`
Expected: PASS.

- [ ] **Step 4: Commit**
```bash
git add src/pages/PrintPage.tsx
git commit -m "fix(v1.1): man Di in — 'Da gui di in' title case, 'Gom de in (N)', le header mon

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: Pinch-zoom Viewer (PdfView)

**Files:** `src/components/PdfView.tsx` (thay toàn bộ)

- [ ] **Step 1: Thay `src/components/PdfView.tsx` bằng:**

```tsx
import { useEffect, useMemo, useRef, useState } from 'react';
import { Document, Page, pdfjs } from 'react-pdf';

// Worker offline (bundle, no CDN). This exact form built + ran correctly on device.
pdfjs.GlobalWorkerOptions.workerSrc = new URL(
  'pdfjs-dist/build/pdf.worker.min.mjs', import.meta.url,
).toString();

interface Props {
  bytes: Uint8Array;
  initialPage: number;            // trang khôi phục lúc mở
  onPageChange: (page: number, total: number) => void; // báo trang hiện tại để lưu
  jumpTo?: number;                // lệnh nhảy tới trang (đổi giá trị = nhảy)
}

const MAX_ZOOM = 4;

export default function PdfView({ bytes, initialPage, onPageChange, jumpTo }: Props) {
  const [numPages, setNumPages] = useState(0);
  const [zoom, setZoom] = useState(1);            // zoom đã commit (1 = fit-width). State → GIỮ khi lật trang.
  const containerRef = useRef<HTMLDivElement>(null);
  const pagesRef = useRef<HTMLDivElement>(null);
  const restored = useRef(false);
  const zoomRef = useRef(1);
  useEffect(() => { zoomRef.current = zoom; }, [zoom]);

  const fitWidth = Math.min(window.innerWidth - 8, 900);
  const pageWidth = Math.round(fitWidth * zoom); // re-raster nét theo zoom

  // CRITICAL: pdf.js detaches the ArrayBuffer. Build the file object ONCE with a COPY.
  const file = useMemo(() => ({ data: bytes.slice(0) }), [bytes]);

  const scrollToPage = (n: number) => {
    document.getElementById(`pdf-page-${n}`)?.scrollIntoView();
  };

  const onLoad = (d: { numPages: number }) => {
    setNumPages(d.numPages);
    onPageChange(Math.min(initialPage, d.numPages), d.numPages);
  };

  // Khôi phục trang sau khi load xong (zoom=1 lúc mở → scrollIntoView chuẩn).
  useEffect(() => {
    if (numPages > 0 && !restored.current) {
      restored.current = true;
      if (initialPage > 1) setTimeout(() => scrollToPage(initialPage), 50);
    }
  }, [numPages, initialPage]);

  // Lệnh nhảy trang từ ngoài.
  useEffect(() => {
    if (jumpTo && jumpTo >= 1 && jumpTo <= numPages) scrollToPage(jumpTo);
  }, [jumpTo, numPages]);

  // Theo dõi trang hiển thị nhiều nhất -> báo ra ngoài để lưu progress.
  // Keyed [numPages] (không phụ thuộc zoom) → observer quan sát ĐÚNG các #pdf-page-n,
  // đổi zoom chỉ resize box, observer vẫn chạy → page-tracking + nhớ-trang không vỡ.
  useEffect(() => {
    if (numPages === 0 || !containerRef.current) return;
    const obs = new IntersectionObserver(
      (entries) => {
        const visible = entries
          .filter((e) => e.isIntersecting)
          .sort((a, b) => b.intersectionRatio - a.intersectionRatio)[0];
        if (visible) {
          const n = Number((visible.target as HTMLElement).dataset.page);
          if (n) onPageChange(n, numPages);
        }
      },
      { root: containerRef.current, threshold: [0.25, 0.5, 0.75] },
    );
    Array.from({ length: numPages }, (_, i) =>
      document.getElementById(`pdf-page-${i + 1}`),
    ).forEach((el) => el && obs.observe(el));
    return () => obs.disconnect();
  }, [numPages]); // eslint-disable-line react-hooks/exhaustive-deps

  // --- Pinch + double-tap (native listeners để preventDefault được; React touchmove là passive) ---
  useEffect(() => {
    const el = containerRef.current;
    if (!el) return;
    let pinching = false, startDist = 0, startZoom = 1, live = 1, lastTap = 0;
    const dist = (t: TouchList) =>
      Math.hypot(t[0].clientX - t[1].clientX, t[0].clientY - t[1].clientY);

    const onStart = (e: TouchEvent) => {
      if (e.touches.length === 2) {
        pinching = true;
        startDist = dist(e.touches);
        startZoom = zoomRef.current;
        live = zoomRef.current;
      }
    };
    const onMove = (e: TouchEvent) => {
      if (pinching && e.touches.length === 2) {
        e.preventDefault(); // chặn cuộn/zoom-trình-duyệt trong lúc pinch
        const ratio = dist(e.touches) / startDist;
        live = Math.max(1, Math.min(MAX_ZOOM, startZoom * ratio));
        if (pagesRef.current) {
          pagesRef.current.style.transformOrigin = 'top center';
          pagesRef.current.style.transform = `scale(${live / zoomRef.current})`; // preview mượt
        }
      }
    };
    const onEnd = (e: TouchEvent) => {
      if (pinching && e.touches.length < 2) {
        pinching = false;
        if (pagesRef.current) pagesRef.current.style.transform = ''; // bỏ preview
        if (Math.abs(live - zoomRef.current) > 0.01) setZoom(live);   // commit → re-raster nét
      } else if (e.touches.length === 0) {
        const now = Date.now();
        if (now - lastTap < 300) { setZoom(1); lastTap = 0; } // double-tap → fit-width
        else lastTap = now;
      }
    };
    el.addEventListener('touchstart', onStart, { passive: false });
    el.addEventListener('touchmove', onMove, { passive: false });
    el.addEventListener('touchend', onEnd);
    return () => {
      el.removeEventListener('touchstart', onStart);
      el.removeEventListener('touchmove', onMove);
      el.removeEventListener('touchend', onEnd);
    };
  }, []);

  return (
    <div
      ref={containerRef}
      style={{ height: '100%', overflow: 'auto', touchAction: 'pan-x pan-y' }}
    >
      {/* width:max-content + margin auto → căn giữa khi vừa khung, pan tới mép khi đã phóng */}
      <div ref={pagesRef} style={{ width: 'max-content', margin: '0 auto' }}>
        <Document file={file} onLoadSuccess={onLoad}>
          {Array.from({ length: numPages }, (_, i) => (
            <div id={`pdf-page-${i + 1}`} data-page={i + 1} key={i} style={{ padding: '6px 0' }}>
              <Page
                pageNumber={i + 1}
                width={pageWidth}
                renderTextLayer={false}
                renderAnnotationLayer={false}
              />
            </div>
          ))}
        </Document>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Build + test**

Run: `npm run build && npm test`
Expected: PASS (ViewerPage lazy → smoke test không kéo react-pdf; 76 test xanh).

- [ ] **Step 3: Commit**
```bash
git add src/components/PdfView.tsx
git commit -m "feat(v1.1): pinch-zoom Viewer (pan + double-tap fit, giu zoom khi lat trang, khong pha nho-trang)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5: Build APK + verify trên Z Flip 4

> Cổng nghiệm thu chính (UI màu + zoom cần mắt + chạm thật).

- [ ] **Step 1: Build + cài**
```bash
source ~/.bash_profile
npm run build && npx cap sync android && (cd android && ./gradlew assembleDebug)
adb -s R5CT844VRCN install -r android/app/build/outputs/apk/debug/app-debug.apk
```
(Dùng debug để nghiệm thu nhanh; bản release ký sẽ build ở Task 6 nếu muốn phát hành.)

- [ ] **Step 2: Checklist (đánh dấu khi đạt)**
  - [ ] Pinch phóng/thu mượt; pan khi đã phóng; double-tap về fit-width.
  - [ ] Lật trang GIỮ NGUYÊN mức zoom; không thu nhỏ dưới fit-width.
  - [ ] Mở lại tài liệu về ĐÚNG trang dừng; "Trang N / M" cập nhật đúng sau zoom + lật (RÀNG BUỘC #2).
  - [ ] KHÔNG còn xanh dương ở bất kỳ nút/nhãn nào — quét cả màn chưa liệt kê (Settings, SyncSettings, FolderPage "+", BackButton…).
  - [ ] "+" môn, "Đóng" sheet, "Xong", "Gom để in", "Nhảy" đều nâu; nút Gom nền nâu đậm + chữ kem.
  - [ ] Tiêu đề sheet "Đang đọc dở" trùng font + màu heading Home.
  - [ ] Badge "Đã gửi đi in" Title case + cùng shade với pill "Đã đồng bộ".
  - [ ] Header nhóm môn màn "Đi in" thẳng lề với row file.
  - [ ] Mọi nhãn/nút Title case (không UPPER/lowercase lạc).

- [ ] **Step 3: Lỗi → debug (systematic-debugging), sửa, build lại. Pass hết → Task 6.**

---

## Task 6: v1.1.0 — quy ước UI vào spec + CHANGELOG + memory + tag

**Files:** `package.json`, `Docs/gu-library-design-spec.md`, `CHANGELOG.md`, memory.

- [ ] **Step 1: Bump version** — `package.json`: `1.0.0` → `1.1.0`.

- [ ] **Step 2: Ghi quy ước UI vào spec**

Thêm vào cuối `Docs/gu-library-design-spec.md`:
```markdown
## Quy ước UI (chốt v1.1.0)
- **Màu hành động (`action`) = nâu đậm `#553B08`** (token `--ion-color-primary`); nút đặc full-width = nền nâu đậm + **chữ kem** (`--ion-color-primary-contrast` = `--gu-cream`). KHÔNG dùng xanh dương Ionic mặc định ở bất kỳ nút/nhãn nào.
- **`success` = MỘT shade xanh lá** (`--ion-color-success`, theo pill sync "Đã đồng bộ") cho mọi trạng thái xong/thành công.
- **Casing = Title case** cho mọi nhãn/nút (global `ion-button { text-transform: none; }`). Không UPPER, không lowercase.
- CSS Ionic phải nạp **trước** `theme/variables.css` (main.tsx) để override token thắng.
```

- [ ] **Step 3: CHANGELOG** — thêm trên `## [1.0.0]`:
```markdown
## [1.1.0] — 2026-06-30 — Pinch-zoom Viewer + đồng nhất UI (token)
### Added
- **Pinch-zoom Viewer:** phóng/thu bằng hai ngón, pan khi đã phóng, double-tap về fit-width; **giữ mức zoom khi lật trang**; không thu dưới fit-width. Re-raster nét theo zoom (width = fitWidth×zoom), preview mượt bằng CSS transform commit khi nhấc tay. Nhớ-trang + "Trang N/M" không đổi (IntersectionObserver giữ nguyên).
### Changed (token layer — giải gốc, không vá rời)
- **Sửa thứ tự CSS:** nạp `@ionic/react/css/*` TRƯỚC `theme/variables.css` (main.tsx) → hết xanh dương rò rỉ (trước đây core.css nạp sau, ghi đè primary nâu về blue).
- **`action` = nâu đậm `#553B08`** + chữ kem cho nút đặc; áp đồng loạt mọi nút/icon/back-button (không sửa rời).
- **`success` = một shade** (pill sync) dùng chung cho badge "Đã gửi đi in".
- **Title case toàn app** (`ion-button` text-transform none): "Đóng"/"Xong"/"Gom để in"… hết all-caps.
- Tiêu đề sheet "Đang đọc dở" trùng font+màu heading Home; nút Gom bỏ tên kỹ thuật `_print/` → "Gom để in (N)"; căn lề header nhóm môn màn "Đi in".
> Verify Z Flip 4 (R5CT844VRCN): zoom mượt + giữ khi lật + nhớ-trang đúng; không còn xanh dương; casing Title case toàn app.
```

- [ ] **Step 4: Memory** — cập nhật `gu-library-m1-toolchain.md`: thêm beat v1.1.0 (pinch-zoom cơ chế width×zoom + commit-on-touchend; **GOTCHA thứ-tự-CSS** Ionic phải nạp trước variables.css; 3 token action/success/casing; quy ước UI ở design-spec). Đổi version hiện tại → v1.1.0. Cập nhật `MEMORY.md`.

- [ ] **Step 5: (tùy chọn) build bản RELEASE ký để phát hành**
```bash
npm run build && npx cap sync android && (cd android && ./gradlew assembleRelease)
# app-release.apk ký cùng keystore — gửi/cài như v1.0.0
```

- [ ] **Step 6: Commit + tag + push**
```bash
git add package.json Docs/gu-library-design-spec.md CHANGELOG.md
git commit -m "chore(v1.1): release v1.1.0 — pinch-zoom + UI token consistency — verified on device

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
git tag v1.1.0
git push && git push --tags
```

---

## Self-Review

**Spec coverage:** token action/success/casing (Task 1) ✅; pinch-zoom + pan + double-tap + giữ-zoom + min fit-width (Task 4) ✅; ràng buộc nhớ-trang (Task 4 thiết kế + Task 5 verify) ✅; "+" môn màu (Task 1 token) ✅; sheet title+Đóng (Task 1+2) ✅; Gom label+màu (Task 1+3) ✅; badge casing+shade+Xong (Task 1+3) ✅; lề header (Task 3) ✅; quy ước vào spec (Task 6) ✅; ngoài phạm vi (tên hiển thị) KHÔNG đụng ✅; `npm run build` là cổng mọi task ✅.

**Placeholder scan:** không TBD; mọi bước có code/đường dẫn/lệnh. ✅

**Rủi ro đã tính:** (1) React touchmove passive → dùng native listener `{passive:false}` để `preventDefault`. (2) flex-center clip khi pan → wrapper `width:max-content;margin:0 auto`. (3) re-raster mọi trang mỗi frame → preview transform + commit-on-end. (4) IO keyed `[numPages]` (không zoom) → page-tracking sống. (5) `touchAction:'pan-x pan-y'` để chặn pinch-zoom trình duyệt nhưng vẫn pan.

**Token consistency:** `--ion-color-primary #553B08` + contrast `--gu-cream`; `--ion-color-success #2dd36f` family; `ion-button{text-transform:none}` — tên biến khớp Ionic. `zoom`/`zoomRef`/`pageWidth`/`pagesRef` dùng nhất quán trong PdfView.
