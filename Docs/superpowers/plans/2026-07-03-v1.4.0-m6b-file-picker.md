# v1.4.0 — M6b: nhập file dự phòng qua file picker (tab "Thêm") — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans. Steps dùng checkbox.

**Goal:** Tab "Thêm" có nút "+" mở system file picker (multi-select, whitelist pdf/doc/docx/ppt/pptx) → **tái dùng nguyên sheet chọn đích v1.3.0** → copy cả lô vào `_inbox/` qua **đường chung với Share** (tiền tố lồng, dedup, strip-tmp, mime). Không đụng worker.

**Architecture:** Thêm native `Saf.pickFiles()` (ACTION_OPEN_DOCUMENT + ALLOW_MULTIPLE + EXTRA_MIME_TYPES) trả `{files:[{uri,name}]}` (cùng shape `SharedFile`). Tách orchestration copy-lô thành `importBatch()` (inboxRepo) + component dùng chung `ImportDestinationFlow` (render `ChooseMonSheet` + toast). `ShareReceiver` và `AddPage` cùng dùng flow đó → **một đường copy duy nhất**.

**Tech Stack:** Native Capacitor plugin Java (Saf), Ionic React, SAF content-URI (`copyToDir` mở InputStream — đã dùng cho Share nên chạy luôn cho file picked).

**Bối cảnh đã xác minh:**
- `src/pages/AddStubPage.tsx` = stub (chỉ text). Route `/add` trong `App.tsx`.
- `src/plugins/shareTarget.ts`: `SharedFile = {uri,name}`.
- `SafPlugin.java`: có `pickFolder` (startActivityForResult + `@ActivityCallback`), `copyToDir` (mở InputStream từ content-URI, dedup `(k)` trước đuôi, mime đúng theo đuôi); import `JSArray/JSObject/Activity/ActivityResult` sẵn.
- `MainActivity.queryName(uri)` dùng `OpenableColumns.DISPLAY_NAME` — cần bản tương tự trong SafPlugin cho file picked.
- `ShareReceiver.pick`: loop `importSharedFile(f.uri,f.name,path)` + `emitKhoChanged` + toast; render `ChooseMonSheet`.
- `importSharedFile(srcUri,name,path[])` (v1.3.0) = đường copy `_inbox/`.
- Whitelist mime khớp `SafPlugin.mimeForName`: pdf/msword/docx/ms-powerpoint/pptx.

---

## File Structure
**Modify:** `android/app/.../SafPlugin.java` (pickFiles + queryDisplayName), `src/plugins/saf.ts` (pickFiles), `src/import/inboxRepo.ts` (importBatch), `src/import/ShareReceiver.tsx` (dùng flow chung), `src/App.tsx` (route /add → AddPage), `package.json`/`CHANGELOG`/memory.
**Create:** `src/import/ImportDestinationFlow.tsx` (sheet + toast dùng chung), `src/pages/AddPage.tsx` (nút "+" + picker).

---

## Task 1: Native `Saf.pickFiles` (multi-select + whitelist)

**Files:** `android/app/src/main/java/com/gulibrary/app/SafPlugin.java`, `src/plugins/saf.ts`

- [ ] **Step 1: Thêm `pickFiles` + callback + helper vào SafPlugin.java** (trước `}` cuối class):
```java
    // Mở system file picker: nhiều file, chỉ loại worker nhận (pdf/doc/docx/ppt/pptx).
    @PluginMethod
    public void pickFiles(PluginCall call) {
        Intent intent = new Intent(Intent.ACTION_OPEN_DOCUMENT);
        intent.addCategory(Intent.CATEGORY_OPENABLE);
        intent.setType("*/*");
        intent.putExtra(Intent.EXTRA_MIME_TYPES, new String[]{
            "application/pdf",
            "application/msword",
            "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
            "application/vnd.ms-powerpoint",
            "application/vnd.openxmlformats-officedocument.presentationml.presentation"
        });
        intent.putExtra(Intent.EXTRA_ALLOW_MULTIPLE, true);
        intent.addFlags(Intent.FLAG_GRANT_READ_URI_PERMISSION);
        startActivityForResult(call, intent, "filesPicked");
    }

    @ActivityCallback
    private void filesPicked(PluginCall call, ActivityResult result) {
        if (call == null) return;
        com.getcapacitor.JSArray files = new com.getcapacitor.JSArray();
        if (result.getResultCode() == Activity.RESULT_OK && result.getData() != null) {
            Intent data = result.getData();
            android.content.ClipData clip = data.getClipData();
            if (clip != null) {
                for (int i = 0; i < clip.getItemCount(); i++) {
                    android.net.Uri uri = clip.getItemAt(i).getUri();
                    if (uri != null) files.put(fileMeta(uri));
                }
            } else if (data.getData() != null) {
                files.put(fileMeta(data.getData()));
            }
        }
        com.getcapacitor.JSObject ret = new com.getcapacitor.JSObject();
        ret.put("files", files);
        call.resolve(ret); // huỷ picker → files rỗng → resolve {files:[]} (không side effect)
    }

    private com.getcapacitor.JSObject fileMeta(android.net.Uri uri) {
        com.getcapacitor.JSObject o = new com.getcapacitor.JSObject();
        o.put("uri", uri.toString());
        o.put("name", queryDisplayName(uri));
        return o;
    }

    private String queryDisplayName(android.net.Uri uri) {
        try (android.database.Cursor c = getContext().getContentResolver().query(uri, null, null, null, null)) {
            if (c != null && c.moveToFirst()) {
                int i = c.getColumnIndex(android.provider.OpenableColumns.DISPLAY_NAME);
                if (i >= 0) return c.getString(i);
            }
        } catch (Exception ignored) {}
        return uri.getLastPathSegment();
    }
```

- [ ] **Step 2: Thêm `pickFiles` vào interface `src/plugins/saf.ts`** (sau `pickFolder`):
```ts
  pickFiles(): Promise<{ files: { uri: string; name: string }[] }>;
```

- [ ] **Step 3: Build web** `npm run build` PASS. **Commit:**
```bash
git add android/app/src/main/java/com/gulibrary/app/SafPlugin.java src/plugins/saf.ts
git commit -m "feat(v1.4): native Saf.pickFiles (ACTION_OPEN_DOCUMENT multi + whitelist pdf/doc/ppt)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: `importBatch` + `ImportDestinationFlow` dùng chung (không fork copy)

**Files:** `src/import/inboxRepo.ts`, `src/import/ImportDestinationFlow.tsx` (mới), `src/import/ShareReceiver.tsx`

- [ ] **Step 1: Thêm `importBatch` vào `src/import/inboxRepo.ts`.** Thêm import `emitKhoChanged` (từ `../lib/khoEvents`) ở đầu file rồi thêm:
```ts
// Copy CẢ LÔ vào _inbox/ với cùng một đích (path). Dùng chung cho Share + file picker.
export async function importBatch(
  files: { uri: string; name: string }[], path: string[],
): Promise<{ ok: number; fails: string[] }> {
  let ok = 0;
  const fails: string[] = [];
  for (const f of files) { // tuần tự → dedup "(k)" trước đuôi đúng thứ tự
    try { await importSharedFile(f.uri, f.name, path); ok += 1; }
    catch { fails.push(f.name); }
  }
  if (ok > 0) emitKhoChanged();
  return { ok, fails };
}
```

- [ ] **Step 2: Tạo `src/import/ImportDestinationFlow.tsx`:**
```tsx
import { useIonToast } from '@ionic/react';
import type { SharedFile } from '../plugins/shareTarget';
import ChooseMonSheet from './ChooseMonSheet';
import { importBatch } from './inboxRepo';

// Sheet chọn đích + copy cả lô (dùng chung ShareReceiver + AddPage). batch>0 = mở sheet.
export default function ImportDestinationFlow({ batch, onClear }: { batch: SharedFile[]; onClear: () => void }) {
  const [presentToast] = useIonToast();

  const pick = async (path: string[]) => {
    const files = batch;
    onClear();
    if (files.length === 0) return;
    const label = path.join(' / ');
    const { ok, fails } = await importBatch(files, path);
    if (fails.length === 0) {
      await presentToast({ message: `Đã thêm ${ok} file vào ${label} (chờ xử lý)`, duration: 2500 });
    } else {
      await presentToast({ message: `Thêm ${ok}/${files.length} file vào ${label}; lỗi: ${fails.join(', ')}`, duration: 3500, color: 'danger' });
    }
  };

  const note = batch.length === 0 ? null : batch.length === 1 ? batch[0].name : `${batch.length} file`;

  return <ChooseMonSheet isOpen={batch.length > 0} note={note} onPick={pick} onCancel={onClear} />;
}
```

- [ ] **Step 3: Rút gọn `src/import/ShareReceiver.tsx`** dùng flow chung (thay toàn file):
```tsx
import { useEffect, useState, useCallback } from 'react';
import { App } from '@capacitor/app';
import { ShareTarget, type SharedFile } from '../plugins/shareTarget';
import ImportDestinationFlow from './ImportDestinationFlow';

// Bắt file share (cold-start + resume) → mở sheet chọn đích, copy cả lô (một lô một đích).
export default function ShareReceiver() {
  const [batch, setBatch] = useState<SharedFile[]>([]);

  const check = useCallback(async () => {
    try { const { files } = await ShareTarget.getSharedFiles(); if (files.length > 0) setBatch(files); }
    catch { /* ignore */ }
  }, []);

  useEffect(() => {
    check();
    const sub = App.addListener('resume', () => { check(); });
    return () => { sub.then((h) => h.remove()); };
  }, [check]);

  return <ImportDestinationFlow batch={batch} onClear={() => setBatch([])} />;
}
```

- [ ] **Step 4: Build + test** `npm run build && npm test` PASS (Share không hồi quy). **Commit:**
```bash
git add src/import/inboxRepo.ts src/import/ImportDestinationFlow.tsx src/import/ShareReceiver.tsx
git commit -m "refactor(v1.4): importBatch + ImportDestinationFlow dung chung (Share + picker, mot duong copy)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: `AddPage` — nút "+" mở picker

**Files:** `src/pages/AddPage.tsx` (mới), `src/App.tsx`

- [ ] **Step 1: Tạo `src/pages/AddPage.tsx`:**
```tsx
import { useState } from 'react';
import {
  IonPage, IonHeader, IonToolbar, IonTitle, IonContent, IonButton, IonIcon,
} from '@ionic/react';
import { addCircleOutline } from 'ionicons/icons';
import { Saf } from '../plugins/saf';
import type { SharedFile } from '../plugins/shareTarget';
import ImportDestinationFlow from '../import/ImportDestinationFlow';

export default function AddPage() {
  const [batch, setBatch] = useState<SharedFile[]>([]);

  const pickFiles = async () => {
    try {
      const { files } = await Saf.pickFiles();
      if (files.length > 0) setBatch(files); // huỷ picker → files rỗng → không mở sheet
    } catch { /* huỷ / lỗi → không side effect */ }
  };

  return (
    <IonPage>
      <IonHeader><IonToolbar><IonTitle>Thêm</IonTitle></IonToolbar></IonHeader>
      <IonContent className="ion-padding">
        <p style={{ color: 'var(--gu-grey)' }}>
          Chọn file từ máy (PDF/Word/PowerPoint) để nhập vào kho. Sau khi chọn, chọn môn/thư mục đích.
        </p>
        <IonButton expand="block" shape="round" onClick={pickFiles}>
          <IonIcon slot="start" icon={addCircleOutline} />
          Chọn file từ máy
        </IonButton>
        <ImportDestinationFlow batch={batch} onClear={() => setBatch([])} />
      </IonContent>
    </IonPage>
  );
}
```

- [ ] **Step 2: Route `/add` → AddPage trong `src/App.tsx`.** Đổi import `AddStubPage` → `AddPage from './pages/AddPage'`; đổi `<Route exact path="/add" component={AddStubPage} />` → `component={AddPage}`. Xoá file `src/pages/AddStubPage.tsx`.

- [ ] **Step 3: Build + test** `npm run build && npm test` PASS. **Commit:**
```bash
git add src/pages/AddPage.tsx src/App.tsx
git rm src/pages/AddStubPage.tsx
git commit -m "feat(v1.4): tab Them — nut '+' mo file picker -> sheet dich (M6b)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: Build APK + verify Z Flip 4 (QA)
- [ ] `npm run build && npx cap sync android && (cd android && ./gradlew assembleRelease)`; `adb install -r`.
- [ ] Checklist:
  - [ ] "Thêm" → "+" → picker mở, chọn 1 pdf → sheet đích → môn phẳng/gốc: file `_inbox/` tiền tố một ngoặc, ⏳ đúng môn, badge cập nhật ngay.
  - [ ] Nhiều file (pdf+docx+pptx) → cùng một đích, tiền tố giống nhau, trùng tên `(k)` trước đuôi.
  - [ ] Đích thư mục con (drill 2 cấp) → tiền tố 2 ngoặc; worker xử → cặp đúng thư mục con.
  - [ ] Picker không cho chọn ảnh/zip (whitelist).
  - [ ] Huỷ picker + huỷ sheet → `_inbox/` sạch.
  - [ ] Share Intent (1 + nhiều file) không hồi quy.
  - [ ] Cài đặt hiện "Phiên bản 1.4.0".

---

## Task 5: v1.4.0 bump + CHANGELOG + memory + tag
- [ ] `package.json` → 1.4.0. CHANGELOG [1.4.0]. Memory (@v1.4.0; `Saf.pickFiles`; `importBatch`/`ImportDestinationFlow` dùng chung). Build release, cài, commit + `git tag v1.4.0` + push.

---

## Self-Review
- Nút "+" tab "Thêm" (Option A, chỉ tab Thêm) ✅; multi-select + whitelist (Task 1 native) ✅; tái dùng nguyên sheet v1.3.0 (ImportDestinationFlow → ChooseMonSheet) ✅; một lô một đích ✅; đường copy chung (importSharedFile qua importBatch, KHÔNG fork) ✅; huỷ picker/sheet không side effect (files rỗng → không mở sheet; onCancel không ghi) ✅; không đụng Share Intent (ShareReceiver chỉ đổi để DÙNG flow chung, hành vi giữ) ✅; token/thẻ-rời/Title-case giữ (không đẻ token) ✅.
- Placeholder: không TBD. Type: `SharedFile{uri,name}` khớp `pickFiles` return + `importBatch` param + `ImportDestinationFlow` prop; `importBatch(files,path[])` khớp; route `/add`→AddPage. ✅
