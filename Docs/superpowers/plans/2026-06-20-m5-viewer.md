# M5 — Viewer (xem PDF + nhảy trang) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
> **Version:** M5 hoàn tất = **minor → 0.5.0** (cập nhật `package.json` + `CHANGELOG.md` ở task chốt).

**Goal:** Thay `ViewerPlaceholderPage` bằng viewer PDF thật: mở PDF từ folder kho (qua SAF content-URI), cuộn đọc nhiều trang mượt, nhảy thẳng tới trang N, và **nhớ + khôi phục trang đang đọc** (nối card "đang đọc dở" — dùng lại `getProgressFor`/`setProgress` đã có). PPTX đã thành PDF nên dùng chung viewer.

**Architecture:** Render bằng **pdf.js trong WebView** qua **react-pdf** (wrapper React của `pdfjs-dist`) — render canvas, nhận bytes trực tiếp (`{ data: Uint8Array }`), chạy offline (worker bundle cục bộ). PDF đọc từ SAF bằng cách **mở rộng plugin `Saf` thêm `readFileBase64`** (file nhị phân → base64 → `Uint8Array`) vì `readFile` hiện chỉ trả UTF-8 text. Trang đang đọc = trang hiển thị nhiều nhất (IntersectionObserver); nhảy trang = scroll tới phần tử trang.

**Tech Stack:** `react-pdf` + `pdfjs-dist` (bundle worker offline qua Vite) · plugin `Saf` (thêm `readFileBase64`) · `src/reading/progress.ts` (đã có) · React + Ionic + Vitest (đã có).

**Phạm vi:** CHỈ M5. **KHÔNG** làm highlight passage (spec mục 6/14.1 gắn cờ "cần thử nghiệm" — thuộc **Phase 2**, KHÔNG chặn MVP). **KHÔNG** đụng M6 (Import/Share). **KHÔNG** đụng search/cross-link. Chỉ: render + cuộn + nhảy trang + nhớ trang. Tài liệu mở từ content-URI SAF (quyền đọc đã có từ M2) — KHÔNG giả định path thường.

**Tiền đề:** M1–M4 xong (verify trên SM-S908E). Có: `Saf` plugin (`src/plugins/saf.ts`: pickFolder/hasPermission/listFolder/readFile), `repo.listFolder`, route `/viewer/:uri` (param base64url qua `src/storage/uriParam.ts`), `progress.ts` (`getProgressFor`/`setProgress`), `monPath.monNameFromUris`. FolderPage mở tài liệu bằng `history.push('/viewer/'+encodeUriParam(d.pdfUri))`.

**Quyết định mở đã chốt (spec mục 15):** thư viện render PDF = **react-pdf (pdf.js)**, sau khi **spike kiểm chứng trên máy thật** (Phase 0). Lý do loại phương án khác: viewer OS/native mở ngoài app KHÔNG báo lại được trang hiện tại → vỡ "nhớ trang/nhảy trang"; ngx-extended-pdf-viewer là Angular. Nếu spike cho thấy react-pdf giật/lỗi nặng trên thiết bị → fallback **pdfjs-dist trực tiếp** (cùng engine, tự render canvas), KHÔNG đổi sang native.

---

## File Structure (M5)

- `android/app/src/main/java/com/gulibrary/app/SafPlugin.java` — **sửa**: thêm `@PluginMethod readFileBase64`.
- `src/plugins/saf.ts` — **sửa**: thêm `readFileBase64` vào interface.
- `src/storage/bytes.ts` + `bytes.test.ts` — **tạo**: base64 → `Uint8Array` (thuần, test được).
- `src/storage/safFile.ts` — **tạo**: `readPdfBytes(uri)` = gọi Saf.readFileBase64 → bytes.
- `src/pages/ViewerPlaceholderPage.tsx` — **xoá**, thay bằng:
- `src/pages/ViewerPage.tsx` — **tạo**: viewer PDF thật.
- `src/components/PdfView.tsx` — **tạo**: render react-pdf (Document/Page) + theo dõi trang + nhảy trang.
- `src/pages/ViewerSpike.tsx` — **tạo ở Phase 0, xoá ở cuối** (throwaway de-risk).
- `src/App.tsx` — **sửa**: route `/viewer/:uri` trỏ `ViewerPage` (+ tạm `/spike` ở Phase 0).
- `src/theme/pdf.css` (hoặc import CSS react-pdf) — **tạo/sửa**: layout trang.

---

## Phase 0 — Spike react-pdf + SAF bytes (GATE trên máy thật)

> **Mục đích:** chứng minh trên thiết bị thật: (a) đọc được bytes PDF từ SAF content-URI, (b) react-pdf render PDF nhiều trang, cuộn mượt, (c) nhảy tới trang N. ĐẠT thì chốt react-pdf và xây tiếp; KHÔNG đạt (giật/lỗi nặng) → thử pdfjs-dist trực tiếp trước khi bỏ phương án WebView. DỪNG nếu cả hai fail (báo người dùng).

### Task 0.1: Mở rộng Saf đọc nhị phân (base64)

**Files:** `android/.../SafPlugin.java`, `src/plugins/saf.ts`

- [ ] **Step 1: Thêm method readFileBase64 vào SafPlugin.java**

Thêm vào trong class `SafPlugin` (cạnh `readFile`):

```java
    // Đọc file nhị phân (PDF) -> base64. readFile cũ chỉ hợp text UTF-8.
    @PluginMethod
    public void readFileBase64(PluginCall call) {
        String uriStr = call.getString("uri");
        if (uriStr == null) { call.reject("uri required"); return; }
        try (java.io.InputStream is = getContext().getContentResolver().openInputStream(android.net.Uri.parse(uriStr))) {
            java.io.ByteArrayOutputStream bos = new java.io.ByteArrayOutputStream();
            byte[] buf = new byte[8192];
            int n;
            while ((n = is.read(buf)) != -1) bos.write(buf, 0, n);
            String b64 = android.util.Base64.encodeToString(bos.toByteArray(), android.util.Base64.NO_WRAP);
            com.getcapacitor.JSObject ret = new com.getcapacitor.JSObject();
            ret.put("data", b64);
            call.resolve(ret);
        } catch (Exception e) {
            call.reject("read failed: " + e.getMessage());
        }
    }
```

- [ ] **Step 2: Thêm vào bridge src/plugins/saf.ts (interface SafPlugin)**

```ts
  readFileBase64(options: { uri: string }): Promise<{ data: string }>;
```

- [ ] **Step 3: Verify native compile + TS build**

Run: `bash -lc 'cd /Users/lavopavden/Dev/projects/Gu-Library/android && ./gradlew :app:compileDebugJavaWithJavac 2>&1 | tail -3'` → BUILD SUCCESSFUL.
Run: `cd /Users/lavopavden/Dev/projects/Gu-Library && npm run build` → PASS.

- [ ] **Step 4: Commit**

```bash
cd /Users/lavopavden/Dev/projects/Gu-Library
git add -A && git commit -m "feat(m5): Saf.readFileBase64 for binary (PDF) reads"
```

### Task 0.2: Cài react-pdf + spike màn render từ SAF

**Files:** `package.json`, `src/pages/ViewerSpike.tsx`, `src/App.tsx` (route tạm), `src/storage/bytes.ts`

- [ ] **Step 1: Cài react-pdf**

```bash
cd /Users/lavopavden/Dev/projects/Gu-Library
npm install react-pdf pdfjs-dist
```

- [ ] **Step 2: Helper base64 → bytes (sẽ giữ lại cho Phase 1)**

Tạo `src/storage/bytes.ts`:
```ts
export function base64ToBytes(b64: string): Uint8Array {
  const bin = atob(b64);
  const out = new Uint8Array(bin.length);
  for (let i = 0; i < bin.length; i++) out[i] = bin.charCodeAt(i);
  return out;
}
```

- [ ] **Step 3: Spike màn (throwaway) render trang đầu + nhảy trang**

Tạo `src/pages/ViewerSpike.tsx`:
```tsx
import { useEffect, useState } from 'react';
import { IonPage, IonContent, IonButton, IonInput, IonItem } from '@ionic/react';
import { Document, Page, pdfjs } from 'react-pdf';
import { getRootUri, listFolder } from '../storage/repo';
import { Saf } from '../plugins/saf';
import { base64ToBytes } from '../storage/bytes';

// Worker offline (bundle, không CDN).
pdfjs.GlobalWorkerOptions.workerSrc = new URL(
  'pdfjs-dist/build/pdf.worker.min.mjs', import.meta.url,
).toString();

export default function ViewerSpike() {
  const [bytes, setBytes] = useState<Uint8Array | null>(null);
  const [numPages, setNumPages] = useState(0);
  const [target, setTarget] = useState('1');
  const [err, setErr] = useState('');

  // Lấy đại PDF đầu tiên tìm được trong kho để thử.
  useEffect(() => {
    (async () => {
      try {
        const root = await getRootUri();
        if (!root) { setErr('chưa chọn kho'); return; }
        // tìm pdf đầu tiên: duyệt môn cấp 1 -> listFolder -> documents[0]
        const top = await listFolder(root);
        let pdfUri: string | null = null;
        for (const f of top.folders) {
          const l = await listFolder(f.uri);
          if (l.documents[0]) { pdfUri = l.documents[0].pdfUri; break; }
          for (const sf of l.folders) {
            const l2 = await listFolder(sf.uri);
            if (l2.documents[0]) { pdfUri = l2.documents[0].pdfUri; break; }
          }
          if (pdfUri) break;
        }
        if (!pdfUri) { setErr('không thấy PDF nào trong kho'); return; }
        const { data } = await Saf.readFileBase64({ uri: pdfUri });
        setBytes(base64ToBytes(data));
      } catch (e: any) { setErr(String(e?.message ?? e)); }
    })();
  }, []);

  const jump = () => {
    const n = parseInt(target, 10);
    document.getElementById(`spike-page-${n}`)?.scrollIntoView({ behavior: 'smooth' });
  };

  return (
    <IonPage>
      <IonContent>
        {err && <p style={{ color: 'red' }}>Lỗi: {err}</p>}
        <IonItem>
          <IonInput value={target} onIonInput={(e) => setTarget(e.detail.value ?? '1')} />
          <IonButton onClick={jump}>Nhảy trang</IonButton>
          <span>/ {numPages}</span>
        </IonItem>
        {bytes && (
          <Document file={{ data: bytes }} onLoadSuccess={(d) => setNumPages(d.numPages)} onLoadError={(e) => setErr(e.message)}>
            {Array.from({ length: numPages }, (_, i) => (
              <div id={`spike-page-${i + 1}`} key={i}>
                <Page pageNumber={i + 1} width={Math.min(window.innerWidth, 800)} />
              </div>
            ))}
          </Document>
        )}
      </IonContent>
    </IonPage>
  );
}
```

- [ ] **Step 4: Route tạm /spike trong App.tsx**

Thêm `<Route exact path="/spike" component={ViewerSpike} />` vào `IonRouterOutlet` (tạm, gỡ ở Phase 3).

- [ ] **Step 5: Build + cài + chạy thử trên máy**

```bash
bash -lc 'cd /Users/lavopavden/Dev/projects/Gu-Library && npm run build && npx cap sync android && cd android && ./gradlew assembleDebug && adb install -r app/build/outputs/apk/debug/app-debug.apk'
```
Mở app, điều hướng tới `/spike` (tạm: có thể tạm cho card/space gọi `history.push('/spike')`, hoặc gõ deep link — đơn giản nhất: tạm thêm 1 IonButton ở HomePage gọi `/spike`, gỡ sau).

- [ ] **Step 6: NGHIỆM THU SPIKE (thao tác tay trên máy)** — bỏ sẵn 1 PDF nhiều trang vào kho.
  1. Màn /spike hiển thị PDF (trang đầu render rõ).
  2. Cuộn xuống — nhiều trang render, cuộn **mượt** (chấp nhận lazy render).
  3. Gõ số trang + "Nhảy trang" → cuộn đúng tới trang đó.

✅ **CỔNG SPIKE:** 3 điểm đạt với react-pdf → chốt react-pdf, sang Phase 1. Nếu render lỗi/giật nặng → thử đổi sang pdfjs-dist trực tiếp (tự render canvas trong cùng ViewerSpike) trước khi sang Phase 1; nếu vẫn fail → DỪNG, báo người dùng.

- [ ] **Step 7: Commit spike**

```bash
git add -A && git commit -m "spike(m5): react-pdf rendering PDF from SAF bytes on device"
```

---

## Phase 1 — SAF binary read (giữ lại, có test)

### Task 1.1: bytes helper test + safFile wrapper

**Files:** `src/storage/bytes.test.ts`, `src/storage/safFile.ts`

- [ ] **Step 1: Test thất bại trước cho base64ToBytes**

Tạo `src/storage/bytes.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { base64ToBytes } from './bytes';

describe('base64ToBytes', () => {
  it('decodes base64 to exact byte sequence', () => {
    // "PDF" = 0x50 0x44 0x46 ; base64 "UERG"
    expect(Array.from(base64ToBytes('UERG'))).toEqual([0x50, 0x44, 0x46]);
  });
  it('decodes the PDF magic header %PDF- (JVBERi0=)', () => {
    expect(Array.from(base64ToBytes('JVBERi0='))).toEqual([0x25, 0x50, 0x44, 0x46, 0x2d]);
  });
  it('empty string -> empty array', () => {
    expect(base64ToBytes('').length).toBe(0);
  });
});
```
(`bytes.ts` đã tạo ở Phase 0; test này khoá hành vi. Nếu chạy trước khi có file thì FAIL.)

- [ ] **Step 2: Chạy test**

Run: `cd /Users/lavopavden/Dev/projects/Gu-Library && npx vitest run src/storage/bytes.test.ts` → PASS (3).

- [ ] **Step 3: Wrapper safFile.ts**

Tạo `src/storage/safFile.ts`:
```ts
import { Saf } from '../plugins/saf';
import { base64ToBytes } from './bytes';

// Đọc một file (PDF) trong kho qua SAF content-URI -> bytes cho pdf.js.
export async function readPdfBytes(uri: string): Promise<Uint8Array> {
  const { data } = await Saf.readFileBase64({ uri });
  return base64ToBytes(data);
}
```

- [ ] **Step 4: build**

Run: `npm run build` → PASS.

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(m5): base64ToBytes (tested) + readPdfBytes SAF wrapper"
```

---

## Phase 2 — Viewer PDF thật

### Task 2.1: PdfView (render + theo dõi trang + nhảy trang)

**Files:** `src/components/PdfView.tsx`

- [ ] **Step 1: Tạo PdfView.tsx**

```tsx
import { useEffect, useRef, useState } from 'react';
import { Document, Page, pdfjs } from 'react-pdf';
import 'react-pdf/dist/Page/AnnotationLayer.css';
import 'react-pdf/dist/Page/TextLayer.css';

pdfjs.GlobalWorkerOptions.workerSrc = new URL(
  'pdfjs-dist/build/pdf.worker.min.mjs', import.meta.url,
).toString();

interface Props {
  bytes: Uint8Array;
  initialPage: number;           // trang khôi phục lúc mở
  onPageChange: (page: number, total: number) => void; // báo trang hiện tại để lưu
  jumpTo?: number;               // lệnh nhảy tới trang (đổi giá trị = nhảy)
}

export default function PdfView({ bytes, initialPage, onPageChange, jumpTo }: Props) {
  const [numPages, setNumPages] = useState(0);
  const containerRef = useRef<HTMLDivElement>(null);
  const restored = useRef(false);

  // file prop ổn định để react-pdf không reload mỗi render.
  const fileProp = useRef({ data: bytes });

  const scrollToPage = (n: number) => {
    document.getElementById(`pdf-page-${n}`)?.scrollIntoView();
  };

  // Khôi phục trang sau khi load xong.
  const onLoad = (d: { numPages: number }) => {
    setNumPages(d.numPages);
    onPageChange(Math.min(initialPage, d.numPages), d.numPages);
  };

  // Sau khi numPages có + chưa khôi phục -> cuộn tới initialPage.
  useEffect(() => {
    if (numPages > 0 && !restored.current) {
      restored.current = true;
      if (initialPage > 1) setTimeout(() => scrollToPage(initialPage), 50);
    }
  }, [numPages, initialPage]);

  // Lệnh nhảy trang từ ngoài.
  useEffect(() => {
    if (jumpTo && jumpTo >= 1 && jumpTo <= numPages) scrollToPage(jumpTo);
  }, [jumpTo, numPages]);

  // Theo dõi trang hiển thị nhiều nhất -> báo ra ngoài để lưu progress.
  useEffect(() => {
    if (numPages === 0 || !containerRef.current) return;
    const obs = new IntersectionObserver(
      (entries) => {
        const visible = entries
          .filter((e) => e.isIntersecting)
          .sort((a, b) => b.intersectionRatio - a.intersectionRatio)[0];
        if (visible) {
          const n = Number((visible.target as HTMLElement).dataset.page);
          if (n) onPageChange(n, numPages);
        }
      },
      { root: containerRef.current, threshold: [0.25, 0.5, 0.75] },
    );
    numPages > 0 && Array.from({ length: numPages }, (_, i) =>
      document.getElementById(`pdf-page-${i + 1}`)).forEach((el) => el && obs.observe(el));
    return () => obs.disconnect();
  }, [numPages]); // eslint-disable-line react-hooks/exhaustive-deps

  return (
    <div ref={containerRef} style={{ height: '100%', overflowY: 'auto' }}>
      <Document file={fileProp.current} onLoadSuccess={onLoad}>
        {Array.from({ length: numPages }, (_, i) => (
          <div id={`pdf-page-${i + 1}`} data-page={i + 1} key={i} style={{ display: 'flex', justifyContent: 'center', padding: '6px 0' }}>
            <Page pageNumber={i + 1} width={Math.min(window.innerWidth - 8, 900)} renderTextLayer={false} renderAnnotationLayer={false} />
          </div>
        ))}
      </Document>
    </div>
  );
}
```

> Ghi chú: `renderTextLayer/AnnotationLayer = false` cho MVP (nhẹ + mượt; text-layer phục vụ highlight là Phase 2). Nếu PDF rất nhiều trang gây nặng, ghi chú để Phase sau virtualize — MVP render tuần tự đủ cho slide bài giảng.

- [ ] **Step 2: build** → `npm run build` PASS.
- [ ] **Step 3: Commit** → `git add -A && git commit -m "feat(m5): PdfView (react-pdf scroll + page tracking + jump)"`

### Task 2.2: ViewerPage (thay placeholder, nối progress)

**Files:** `src/pages/ViewerPage.tsx`, xoá `src/pages/ViewerPlaceholderPage.tsx`, sửa `src/App.tsx`

- [ ] **Step 1: Tạo ViewerPage.tsx**

```tsx
import { useEffect, useRef, useState } from 'react';
import {
  IonPage, IonHeader, IonToolbar, IonTitle, IonButtons, IonBackButton, IonContent,
  IonInput, IonButton, IonText,
} from '@ionic/react';
import { useParams } from 'react-router-dom';
import PdfView from '../components/PdfView';
import { readPdfBytes } from '../storage/safFile';
import { getRootUri } from '../storage/repo';
import { getProgressFor, setProgress } from '../reading/progress';
import { decodeUriParam } from '../storage/uriParam';
import { monNameFromUris } from '../storage/monPath';

function baseName(contentUri: string): string {
  const last = decodeURIComponent(contentUri).split('/').pop() ?? contentUri;
  return last.replace(/\.[^.]+$/, '');
}

export default function ViewerPage() {
  const { uri } = useParams<{ uri: string }>();
  const docUri = decodeUriParam(uri);
  const name = baseName(docUri);

  const [bytes, setBytes] = useState<Uint8Array | null>(null);
  const [initialPage, setInitialPage] = useState<number | null>(null);
  const [monName, setMonName] = useState('Tài liệu');
  const [jumpTo, setJumpTo] = useState<number | undefined>(undefined);
  const [target, setTarget] = useState('');
  const [err, setErr] = useState('');
  const lastSaved = useRef(0);

  useEffect(() => {
    let alive = true;
    (async () => {
      try {
        const [prog, root] = await Promise.all([getProgressFor(docUri), getRootUri()]);
        if (!alive) return;
        setInitialPage(prog?.page ?? 1);
        setMonName((root && monNameFromUris(root, docUri)) || prog?.monName || 'Tài liệu');
        setBytes(await readPdfBytes(docUri));
      } catch (e: any) { if (alive) setErr(String(e?.message ?? e)); }
    })();
    return () => { alive = false; };
  }, [docUri]);

  const onPageChange = (page: number, total: number) => {
    if (page === lastSaved.current) return;
    lastSaved.current = page;
    setProgress({ docUri, name, monName, page, total });
  };

  const doJump = () => {
    const n = parseInt(target, 10);
    if (!Number.isNaN(n)) setJumpTo(n); // PdfView useEffect phụ thuộc jumpTo
  };

  return (
    <IonPage>
      <IonHeader>
        <IonToolbar>
          <IonButtons slot="start"><IonBackButton defaultHref="/home" /></IonButtons>
          <IonTitle className="gu-title">{name}</IonTitle>
          <IonButtons slot="end">
            <IonInput
              style={{ maxWidth: 56 }} type="number" placeholder="Tr"
              value={target} onIonInput={(e) => setTarget(e.detail.value ?? '')}
            />
            <IonButton onClick={doJump}>Nhảy</IonButton>
          </IonButtons>
        </IonToolbar>
      </IonHeader>
      <IonContent>
        {err && <IonText color="danger"><p className="ion-padding">Không mở được PDF: {err}</p></IonText>}
        {!err && (!bytes || initialPage == null) && <p className="ion-padding">Đang tải PDF…</p>}
        {bytes && initialPage != null && (
          <PdfView
            bytes={bytes}
            initialPage={initialPage}
            onPageChange={onPageChange}
            jumpTo={jumpTo}
          />
        )}
      </IonContent>
    </IonPage>
  );
}
```

- [ ] **Step 2: Trỏ route /viewer/:uri sang ViewerPage; xoá placeholder**

Trong `src/App.tsx`: đổi import `ViewerPlaceholderPage` → `ViewerPage`, sửa `<Route ... component={ViewerPage} />`, gỡ route `/spike` tạm.
```bash
git rm src/pages/ViewerPlaceholderPage.tsx
grep -rn "ViewerPlaceholderPage\|/spike\|ViewerSpike" src/ || echo "no stale refs"
```
Xoá luôn `src/pages/ViewerSpike.tsx` và nút /spike tạm ở HomePage (nếu đã thêm ở Phase 0).

- [ ] **Step 3: test + build** → `npm test && npm run build` PASS (smoke test còn xanh).
- [ ] **Step 4: Commit** → `git add -A && git commit -m "feat(m5): real ViewerPage (PDF render + jump + remember page); remove placeholder"`

---

## Phase 3 — Build + nghiệm thu trên máy thật

### Task 3.1: Build + install
- [ ] **Step 1:** đảm bảo kho có ≥1 PDF nhiều trang (vd copy 1 PDF thật vào 1 môn, hoặc fixture). 
- [ ] **Step 2:**
```bash
bash -lc 'cd /Users/lavopavden/Dev/projects/Gu-Library && npm run build && npx cap sync android && cd android && ./gradlew assembleDebug && adb install -r app/build/outputs/apk/debug/app-debug.apk'
```
→ BUILD SUCCESSFUL + Success.

### Task 3.2: NGHIỆM THU M5 (thao tác tay, đối chiếu brief)
- [ ] Mở một tài liệu từ Home → môn → tài liệu → **PDF hiện, cuộn nhiều trang mượt**.
- [ ] Gõ số trang + "Nhảy" → **nhảy đúng tới trang N**.
- [ ] Cuộn tới trang giữa → back về Home → **card "đang đọc dở" đúng trang** → bấm card → mở lại **đúng trang đang đọc** (khôi phục).
- [ ] Mở tài liệu trong môn lồng sâu (Chương/Buổi) — đọc được qua content-URI SAF (không chỉ path thường).

✅ **M5 ĐẠT** khi 4 điểm đúng.

### Task 3.3: Chốt version + commit
- [ ] Bump `package.json` → **0.5.0**; thêm mục `## [0.5.0] — M5: Viewer PDF` vào `CHANGELOG.md`.
- [ ] Commit:
```bash
cd /Users/lavopavden/Dev/projects/Gu-Library
git add -A && git commit -m "feat(m5): PDF viewer verified on device (render + jump + remember page) — v0.5.0"
```

---

## Nghiệm thu M5 (đối chiếu build brief)
- [ ] Mở + cuộn được PDF nhiều trang mượt. → Phase 2 + 3.2.
- [ ] Nhảy thẳng tới trang N theo lệnh. → PdfView jumpTo + 3.2.
- [ ] Ghi nhớ + khôi phục đúng trang đang đọc (nối card đang-đọc-dở). → ViewerPage progress + 3.2.

## Self-review notes
- **Brief M5 (render + nhảy + nhớ trang):** PdfView (cuộn react-pdf) + jumpTo + onPageChange↔setProgress/getProgressFor. ✓
- **Đọc qua SAF content-URI (không path thường):** `Saf.readFileBase64` → bytes → react-pdf `{data}`. Phase 0 spike xác minh trên máy. ✓
- **PPTX dùng chung viewer:** viewer chỉ ăn PDF (PPTX đã convert ở M7) — cùng đường. ✓
- **KHÔNG làm highlight (Phase 2):** `renderTextLayer=false`; chỉ render + nhảy. ✓
- **Quyết định thư viện mở (spec 15):** chốt react-pdf SAU spike trên thiết bị (Phase 0 gate), fallback pdfjs-dist trực tiếp; loại native/ngx có lý do. ✓
- **Offline:** worker pdf.js bundle cục bộ (Vite URL), không CDN. ✓
- **Nhớ trang nối card đang-đọc-dở (M4):** dùng lại `progress.ts` + `monPath` — không tạo store mới. ✓
- **TDD:** `base64ToBytes` thuần có test; render/scroll/jump là cổng verify thiết bị (không test trong jsdom — pdf.js/canvas cần môi trường thật). ✓

## Điểm cần người dùng thao tác tay
- Phase 0 Step 6 (spike) + Phase 3.2: bỏ 1 PDF nhiều trang vào kho; nghiệm thu render/cuộn/nhảy/nhớ trang trên SM-S908E.
