# v1.3.0 — Sheet chọn đích Share drill-down (thư mục con, độ sâu bất kỳ) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans. Steps dùng checkbox.

**Goal:** Khi share file vào app, sheet "Lưu vào môn nào?" cho chọn thẳng **thư mục con** (drill độ sâu bất kỳ) + tạo folder mới ngay tại sheet; tiền tố `_inbox/` ghi **lặp ngoặc mỗi cấp** (`[Môn][Con] file.pdf`) khớp worker v0.9.0.

**Architecture:** `ChooseMonSheet` thành navigator drill-down trong CÙNG một IonModal (không sheet chồng sheet): bước 1 = list môn (như cũ); tap môn có con → bước 2+ đệ quy (Lưu-vào-đây + list con + "+ Thư mục mới" + back). `onPick` trả **mảng đoạn đường** `string[]`. Tiền tố (`makeInboxName`) build từ mảng đó. `parseInboxPrefix` lấy **ngoặc đầu = môn** cho badge. Không đụng copy `_inbox/`/dedup/strip-tmp/mime.

**Tech Stack:** Ionic React (IonModal sheet, `expandToScroll=false`, thẻ-rời), Capacitor SAF (`Saf.createDir` qua `repo.createSubfolder`), Vitest cho logic thuần.

**Điều kiện triển khai (ngoài phạm vi code app):** worker **v0.9.0** (hiểu tiền tố lặp ngoặc) PHẢI chạy trên mini PC trước khi cài app này ra máy thật — nếu không file share vào thư mục con bị worker cũ parse sai.

**Bối cảnh đã xác minh:**
- `src/import/prefix.ts`: `makeInboxName(mon, name)`=`[mon] name` (strip-tmp); `parseInboxPrefix` regex `^\[([^\]]+)\]\s(.+)$` (KHÔNG khớp `[A][B]`); `UNFILED='Chưa phân loại'`; `stripTempSuffix`.
- `src/import/inboxRepo.ts`: `importSharedFile(srcUri, originalName, monName)` → `makeInboxName` + `setLastMon`; `listInboxByMon` dùng `parseInboxPrefix().mon`.
- `src/import/ShareReceiver.tsx`: bắt share, `pick(monName)` copy cả lô, toast, `emitKhoChanged`.
- `src/import/ChooseMonSheet.tsx`: IonModal `breakpoints=[0,0.92]` `expandToScroll={false}`; list môn (thẻ giấy) + "Chưa phân loại"; `onPick(monName)`.
- `src/storage/repo.ts`: `listMon()`; `listFolder(uri)→{folders,documents,pending}`; `createSubfolder(parentUri,name)` (`Saf.createDir` reject 'exists' + `emitKhoChanged`).
- `src/storage/folderName.ts`: `validateFolderName(raw)` chặn `/[/\\:*?"<>|]/` + rỗng → `{ok,value}|{ok,error}`.
- Thẻ-rời + `card` style đã có trong ChooseMonSheet.

---

## File Structure
**Modify:**
- `src/import/prefix.ts` — `makeInboxName(path[])` lặp ngoặc; `parseInboxPrefix` chịu nhiều ngoặc (mon=đầu).
- `src/import/prefix.test.ts` — test lặp ngoặc + parse nhiều ngoặc.
- `src/storage/folderName.ts` — chặn thêm `[` `]` + đầu `_`.
- `src/storage/folderName.test.ts` — test ký tự mới.
- `src/import/inboxRepo.ts` — `importSharedFile(..., path: string[])`.
- `src/import/ChooseMonSheet.tsx` — drill-down navigator, `onPick(path: string[])`, inline "+ Thư mục mới".
- `src/import/ShareReceiver.tsx` — `pick(path: string[])`.
- `package.json`, `CHANGELOG.md`, memory.

---

## Task 1: Tiền tố lặp ngoặc (prefix.ts) — TDD

**Files:** `src/import/prefix.ts`, `src/import/prefix.test.ts`

- [ ] **Step 1: Sửa test `makeInboxName` + thêm test lặp ngoặc / parse nhiều ngoặc.**

Trong `src/import/prefix.test.ts`, đổi 3 test `makeInboxName` sang chữ ký mảng + thêm ca lồng; thêm test parse nhiều ngoặc. Thay khối:
```ts
  it('makeInboxName gắn tiền tố môn', () => {
    expect(makeInboxName('Tố tụng Hình sự', 'bai.pdf')).toBe('[Tố tụng Hình sự] bai.pdf');
  });
  it('makeInboxName strip đuôi tạm của tên nguồn', () => {
    expect(makeInboxName('Tố tụng Hình sự', 'bai.pdf.tmp')).toBe('[Tố tụng Hình sự] bai.pdf');
  });
  it('makeInboxName cho Chưa phân loại', () => {
    expect(makeInboxName(UNFILED, 'x.docx')).toBe('[Chưa phân loại] x.docx');
  });
```
bằng:
```ts
  it('makeInboxName một cấp (môn) = một ngoặc', () => {
    expect(makeInboxName(['Tố tụng Hình sự'], 'bai.pdf')).toBe('[Tố tụng Hình sự] bai.pdf');
  });
  it('makeInboxName lồng nhiều cấp = lặp ngoặc mỗi cấp', () => {
    expect(makeInboxName(['Luật Đất đai', 'Bài giảng'], 'file.pdf')).toBe('[Luật Đất đai][Bài giảng] file.pdf');
    expect(makeInboxName(['A', 'B', 'C'], 'x.docx')).toBe('[A][B][C] x.docx');
  });
  it('makeInboxName strip đuôi tạm của tên nguồn', () => {
    expect(makeInboxName(['Tố tụng Hình sự'], 'bai.pdf.tmp')).toBe('[Tố tụng Hình sự] bai.pdf');
  });
  it('makeInboxName cho Chưa phân loại', () => {
    expect(makeInboxName([UNFILED], 'x.docx')).toBe('[Chưa phân loại] x.docx');
  });
```
Và thêm vào `describe('inbox prefix', ...)` (cạnh các test parse):
```ts
  it('parseInboxPrefix lấy NGOẶC ĐẦU làm môn với tiền tố lồng', () => {
    expect(parseInboxPrefix('[Luật Đất đai][Bài giảng] file.pdf'))
      .toEqual({ mon: 'Luật Đất đai', name: 'file.pdf' });
    expect(parseInboxPrefix('[A][B][C] x.docx')).toEqual({ mon: 'A', name: 'x.docx' });
  });
```

- [ ] **Step 2: Chạy test → FAIL.** `npx vitest run src/import/prefix.test.ts` (makeInboxName nhận string[], regex parse cũ trượt lồng).

- [ ] **Step 3: Sửa `src/import/prefix.ts`.** Thay `makeInboxName` + `parseInboxPrefix`:
```ts
// Tên file trong _inbox: lặp ngoặc mỗi cấp đường đích "[Môn][Con]... <tên gốc>" (interface app↔worker v0.9.0).
// path = mảng đoạn đường từ môn xuống (["Luật Đất đai","Bài giảng"]); một cấp = một ngoặc.
export function makeInboxName(path: string[], originalName: string): string {
  const prefix = path.map((seg) => `[${seg}]`).join('');
  return `${prefix} ${stripTempSuffix(originalName)}`;
}

// Tách tiền tố lồng "[Môn][Con]... <tên>" -> {mon = NGOẶC ĐẦU, name}; null nếu không khớp.
export function parseInboxPrefix(fileName: string): { mon: string; name: string } | null {
  const first = /^\[([^\]]+)\]/.exec(fileName);
  if (!first) return null;
  const name = fileName.replace(/^(?:\[[^\]]+\])+\s/, ''); // bỏ mọi nhóm ngoặc đầu + một space
  if (name === fileName) return null; // không có "space + tên" → không phải mẫu của ta
  return { mon: first[1], name };
}
```

- [ ] **Step 4: Chạy test → PASS.** `npx vitest run src/import/prefix.test.ts`.

- [ ] **Step 5: Commit.**
```bash
git add src/import/prefix.ts src/import/prefix.test.ts
git commit -m "feat(v1.3): tien to lap ngoac moi cap ([Mon][Con]); parse lay ngoac dau = mon

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: Chặn ký tự tên folder mới (folderName.ts) — TDD

**Files:** `src/storage/folderName.ts`, `src/storage/folderName.test.ts`

- [ ] **Step 1: Thêm test.** Trong `src/storage/folderName.test.ts`, thêm:
```ts
  it('chặn ngoặc vuông [ ] (phá hợp đồng tiền tố)', () => {
    expect(validateFolderName('Bài[giảng]').ok).toBe(false);
    expect(validateFolderName('a]b').ok).toBe(false);
  });
  it('chặn tên bắt đầu bằng _ (quy ước file hệ thống)', () => {
    expect(validateFolderName('_inbox').ok).toBe(false);
    expect(validateFolderName('_x').ok).toBe(false);
  });
  it('chấp nhận tên hợp lệ có gạch/space', () => {
    const r = validateFolderName('  Bài giảng 2024 ');
    expect(r.ok).toBe(true);
    if (r.ok) expect(r.value).toBe('Bài giảng 2024');
  });
```

- [ ] **Step 2: Chạy test → FAIL.** `npx vitest run src/storage/folderName.test.ts`.

- [ ] **Step 3: Sửa `src/storage/folderName.ts`.** Thay toàn file:
```ts
const FORBIDDEN = /[/\\:*?"<>|[\]]/; // thêm [ ] (phá tiền tố ngoặc)
export type NameResult = { ok: true; value: string } | { ok: false; error: string };
export function validateFolderName(raw: string): NameResult {
  const value = raw.trim();
  if (!value) return { ok: false, error: 'Tên không được rỗng' };
  if (value.startsWith('_')) return { ok: false, error: 'Tên không được bắt đầu bằng "_"' };
  if (FORBIDDEN.test(value)) return { ok: false, error: 'Tên chứa ký tự cấm ( / \\ : * ? " < > | [ ] )' };
  return { ok: true, value };
}
```

- [ ] **Step 4: Chạy test → PASS.** `npm test` (không hồi quy CreateFolderModal/tạo môn).

- [ ] **Step 5: Commit.**
```bash
git add src/storage/folderName.ts src/storage/folderName.test.ts
git commit -m "feat(v1.3): validateFolderName chan [ ] va dau _ (hop dong tien to + file he thong)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: inboxRepo nhận path[] — TDD-nhẹ (typecheck)

**Files:** `src/import/inboxRepo.ts`

- [ ] **Step 1: Sửa `importSharedFile`.** Trong `src/import/inboxRepo.ts`:
```ts
// Copy file share vào _inbox/ với tiền tố đường đích (mảng đoạn từ môn xuống). Trả tên cuối.
export async function importSharedFile(srcUri: string, originalName: string, path: string[]): Promise<string> {
  const inbox = await ensureInbox();
  const { name } = await Saf.copyToDir({ srcUri, dirUri: inbox, name: makeInboxName(path, originalName) });
  await setLastMon(path[0]);
  return name;
}
```
(`listInboxByMon` giữ nguyên — `parseInboxPrefix().mon` giờ đã trả ngoặc đầu cho tiền tố lồng.)

- [ ] **Step 2: Build.** `npm run build` — sẽ báo lỗi type ở ShareReceiver (chưa sửa) → OK, sẽ sửa Task 5. Chạy `npx tsc -b 2>&1 | grep inboxRepo` để chắc inboxRepo tự nó sạch.

- [ ] **Step 3: (commit gộp với Task 5 vì phụ thuộc chéo — bỏ commit riêng ở đây).**

---

## Task 4: ChooseMonSheet drill-down navigator

**Files:** `src/import/ChooseMonSheet.tsx` (viết lại)

Thiết kế: state `stack: {name,uri,folders}[]` ([] = bước 1 root). Bước 1 render list môn; tap môn → `listFolder` → có con thì push (drill), không thì `onPick([name])`. "Chưa phân loại" luôn `onPick([UNFILED])`. Bước 2+ render: "Lưu vào «tên»" + list con + "+ Thư mục mới" (inline) + back ở header.

- [ ] **Step 1: Thay toàn bộ `src/import/ChooseMonSheet.tsx`:**
```tsx
import { useEffect, useState } from 'react';
import type { CSSProperties } from 'react';
import {
  IonModal, IonHeader, IonToolbar, IonTitle, IonButtons, IonButton, IonContent,
  IonList, IonItem, IonLabel, IonNote, IonInput, IonIcon, IonSpinner,
} from '@ionic/react';
import { arrowBack, folderOutline, addCircleOutline, checkmarkCircleOutline } from 'ionicons/icons';
import { listMon, listFolder, createSubfolder } from '../storage/repo';
import { getLastMon } from './inboxRepo';
import { UNFILED } from './prefix';
import { validateFolderName } from '../storage/folderName';
import MonSwatch from '../components/MonSwatch';
import UnfiledSwatch from '../components/UnfiledSwatch';
import type { Mon, SubFolder } from '../storage/types';

interface Props {
  isOpen: boolean;
  note: string | null;
  onPick: (path: string[]) => void;   // đường đích từ môn xuống
  onCancel: () => void;
}

const card: CSSProperties = {
  '--background': 'var(--gu-paper-2)', '--border-radius': '14px',
  '--padding-top': '10px', '--padding-bottom': '10px',
} as CSSProperties;

type Level = { name: string; uri: string; folders: SubFolder[] };

export default function ChooseMonSheet({ isOpen, note, onPick, onCancel }: Props) {
  const [mons, setMons] = useState<Mon[]>([]);
  const [last, setLast] = useState<string | null>(null);
  const [stack, setStack] = useState<Level[]>([]);
  const [busy, setBusy] = useState(false);
  const [newMode, setNewMode] = useState(false);
  const [newName, setNewName] = useState('');
  const [newErr, setNewErr] = useState('');

  useEffect(() => {
    if (!isOpen) return;
    setStack([]); setNewMode(false); setNewName(''); setNewErr(''); setBusy(false);
    (async () => {
      try { setMons(await listMon()); } catch { setMons([]); }
      setLast(await getLastMon());
    })();
  }, [isOpen]);

  const ordered = mons
    .filter((m) => m.name !== UNFILED)
    .sort((a, b) => (a.name === last ? -1 : b.name === last ? 1 : 0));

  const path = stack.map((s) => s.name);
  const cur = stack[stack.length - 1] ?? null;

  // Tap môn ở bước 1: có con → drill, không → lưu thẳng.
  const tapMon = async (m: Mon) => {
    setBusy(true);
    try {
      const folders = (await listFolder(m.uri)).folders;
      if (folders.length === 0) { onPick([m.name]); return; }
      setStack([{ name: m.name, uri: m.uri, folders }]);
    } catch { onPick([m.name]); } finally { setBusy(false); }
  };

  const tapFolder = async (f: SubFolder) => {
    setBusy(true); setNewMode(false); setNewErr('');
    try {
      const folders = (await listFolder(f.uri)).folders;
      setStack((s) => [...s, { name: f.name, uri: f.uri, folders }]);
    } catch { setStack((s) => [...s, { name: f.name, uri: f.uri, folders: [] }]); }
    finally { setBusy(false); }
  };

  const back = () => { setNewMode(false); setNewErr(''); setStack((s) => s.slice(0, -1)); };

  const createNew = async () => {
    const r = validateFolderName(newName);
    if (!r.ok) { setNewErr(r.error); return; }
    if (cur && cur.folders.some((f) => f.name === r.value)) { setNewErr('Đã tồn tại'); return; }
    if (!cur) return;
    setBusy(true);
    try {
      await createSubfolder(cur.uri, r.value); // tạo folder ngay (M6c) — không đợi worker
      onPick([...path, r.value]);              // chọn nó làm đích luôn
    } catch (e: unknown) {
      const msg = e instanceof Error ? e.message : String(e);
      setNewErr(msg.includes('exists') ? 'Đã tồn tại' : (msg || 'Lỗi tạo thư mục'));
      setBusy(false);
    }
  };

  const atRoot = stack.length === 0;

  return (
    <IonModal isOpen={isOpen} onDidDismiss={onCancel}
      breakpoints={[0, 0.92]} initialBreakpoint={0.92} expandToScroll={false}>
      <IonHeader>
        <IonToolbar>
          {!atRoot && (
            <IonButtons slot="start">
              <IonButton onClick={back} aria-label="Quay lại"><IonIcon slot="icon-only" icon={arrowBack} /></IonButton>
            </IonButtons>
          )}
          <IonTitle className="gu-title" style={{ fontSize: 17 }}>
            {atRoot ? 'Lưu vào môn nào?' : `Lưu vào đâu trong ${cur?.name}?`}
          </IonTitle>
        </IonToolbar>
      </IonHeader>
      <IonContent className="ion-padding">
        {note && <IonNote>{note}</IonNote>}
        {busy && <div style={{ textAlign: 'center', padding: 12 }}><IonSpinner /></div>}

        <IonList style={{ background: 'transparent' }}>
          {atRoot ? (
            <>
              {ordered.map((m) => (
                <div key={m.uri} style={{ marginBottom: 10 }}>
                  <IonItem button detail={false} lines="none" disabled={busy} onClick={() => tapMon(m)} style={card}>
                    <MonSwatch name={m.name} color={m.meta.color} icon={m.meta.icon} />
                    <IonLabel className="gu-serif" style={{ marginLeft: 12 }}>
                      {m.name}{m.name === last ? '  · vừa dùng' : ''}
                    </IonLabel>
                  </IonItem>
                </div>
              ))}
              {/* "Chưa phân loại": luôn phẳng, không drill */}
              <div style={{ marginBottom: 10 }}>
                <IonItem button detail={false} lines="none" disabled={busy} onClick={() => onPick([UNFILED])} style={card}>
                  <UnfiledSwatch />
                  <IonLabel color="medium" style={{ marginLeft: 12, fontStyle: 'italic' }}>Chưa phân loại</IonLabel>
                </IonItem>
              </div>
            </>
          ) : (
            <>
              {/* Lưu vào cấp hiện tại */}
              <div style={{ marginBottom: 10 }}>
                <IonItem button detail={false} lines="none" disabled={busy} onClick={() => onPick(path)} style={card}>
                  <IonIcon icon={checkmarkCircleOutline} style={{ color: 'var(--gu-brown)', marginRight: 12 }} />
                  <IonLabel className="gu-serif">Lưu vào “{cur?.name}”</IonLabel>
                </IonItem>
              </div>
              {/* Thư mục con */}
              {cur?.folders.map((f) => (
                <div key={f.uri} style={{ marginBottom: 10 }}>
                  <IonItem button detail={false} lines="none" disabled={busy} onClick={() => tapFolder(f)} style={card}>
                    <IonIcon icon={folderOutline} style={{ color: 'var(--gu-brown)', marginRight: 12 }} />
                    <IonLabel className="gu-serif">{f.name}</IonLabel>
                  </IonItem>
                </div>
              ))}
              {/* + Thư mục mới (inline, không sheet chồng sheet) */}
              {newMode ? (
                <div style={{ ...({ background: 'var(--gu-paper-2)' }), borderRadius: 14, padding: 12, marginBottom: 10 }}>
                  <IonInput value={newName} placeholder="Tên thư mục mới…" disabled={busy}
                    onIonInput={(e) => { setNewName(String(e.detail.value ?? '')); setNewErr(''); }}
                    style={{ border: '1px solid var(--ion-color-medium)', borderRadius: 8, padding: '4px 8px' }} />
                  {newErr && <p style={{ color: 'var(--ion-color-danger)', fontSize: 13, margin: '6px 0 0' }}>{newErr}</p>}
                  <div style={{ display: 'flex', gap: 8, marginTop: 10 }}>
                    <IonButton size="small" fill="clear" disabled={busy} onClick={() => { setNewMode(false); setNewErr(''); }}>Huỷ</IonButton>
                    <IonButton size="small" shape="round" disabled={busy} onClick={createNew}>Tạo &amp; lưu</IonButton>
                  </div>
                </div>
              ) : (
                <div style={{ marginBottom: 10 }}>
                  <IonItem button detail={false} lines="none" disabled={busy}
                    onClick={() => { setNewMode(true); setNewName(''); setNewErr(''); }} style={card}>
                    <IonIcon icon={addCircleOutline} style={{ color: 'var(--gu-brown)', marginRight: 12 }} />
                    <IonLabel className="gu-serif">+ Thư mục mới</IonLabel>
                  </IonItem>
                </div>
              )}
            </>
          )}
        </IonList>
      </IonContent>
    </IonModal>
  );
}
```

- [ ] **Step 2: Build** (kèm Task 3 + 5). `npm run build`.

---

## Task 5: ShareReceiver dùng path[]

**Files:** `src/import/ShareReceiver.tsx`

- [ ] **Step 1: Sửa `pick`.** Thay `const pick = async (monName: string) => {...}` bằng:
```tsx
  const pick = async (path: string[]) => {
    const files = batch;
    setBatch([]);
    if (files.length === 0) return;
    const label = path.join(' / ');
    let ok = 0;
    const fails: string[] = [];
    for (const f of files) {
      try { await importSharedFile(f.uri, f.name, path); ok += 1; }
      catch { fails.push(f.name); }
    }
    if (ok > 0) emitKhoChanged();
    if (fails.length === 0) {
      await presentToast({ message: `Đã thêm ${ok} file vào ${label} (chờ xử lý)`, duration: 2500 });
    } else {
      await presentToast({ message: `Thêm ${ok}/${files.length} file vào ${label}; lỗi: ${fails.join(', ')}`, duration: 3500, color: 'danger' });
    }
  };
```

- [ ] **Step 2: Build + test.** `npm run build && npm test` — PASS (App smoke + logic).

- [ ] **Step 3: Commit (gộp Task 3+4+5).**
```bash
git add src/import/inboxRepo.ts src/import/ChooseMonSheet.tsx src/import/ShareReceiver.tsx
git commit -m "feat(v1.3): sheet chon dich drill-down (thu muc con do sau bat ky) + tao folder tai cho

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 6: Build APK + verify máy thật (worker v0.9.0 đã chạy trước)

- [ ] `npm run build && npx cap sync android && (cd android && ./gradlew assembleRelease)`; `adb install -r`.
- [ ] Checklist:
  - [ ] Share 1 file → môn phẳng: 1 tap lưu thẳng, tiền tố `[Môn]` y v1.2.x.
  - [ ] Môn có con → bước 2; "Lưu vào «Môn»" = tiền tố một ngoặc; tap con → 2 ngoặc; worker đặt đúng thư mục con.
  - [ ] Drill 3 cấp → 3 ngoặc; back từng cấp đúng.
  - [ ] "+ Thư mục mới" → folder hiện ngay cây app + file lưu vào đó; tên `[`, `/`, đầu `_`, rỗng → chặn tại chỗ.
  - [ ] "Chưa phân loại": tap lưu thẳng, không bước 2, không "+ con".
  - [ ] Multi-share cả lô → một đích; dedup `(k)` trước đuôi giữ.
  - [ ] Gesture sheet (kéo cuộn/grabber/over-scroll) không hồi quy; badge ⏳/Chưa-phân-loại đúng.
  - [ ] Cài đặt hiện "Phiên bản 1.3.0".

---

## Task 7: v1.3.0 bump + CHANGELOG + memory + tag
- [ ] `package.json` → 1.3.0.
- [ ] CHANGELOG mục [1.3.0] (drill-down đích Share, tiền tố lặp ngoặc, tạo folder tại sheet, chặn `[]`/`_`).
- [ ] Cập nhật memory + MEMORY.md (@ v1.3.0; hợp đồng tiền tố lồng khớp worker v0.9.0).
- [ ] Build release v1.3.0, cài (máy hiện "1.3.0"). Commit + `git tag v1.3.0` + push (+tags).

---

## Self-Review
- Bước 1 giữ nguyên (tap môn phẳng = pick ngay) ✅; bước 2 có điều kiện (có con mới drill) ✅; drill đệ quy độ sâu bất kỳ + back ✅; "+ Thư mục mới" inline (không sheet chồng) + tạo-rồi-pick ✅; "Chưa phân loại" phẳng ✅; multi-share một đích ✅ (Task 4/5). Tiền tố lặp ngoặc + parse mon=đầu (Task 1) ✅. Chặn `[]`/`/\`/đầu`_`/rỗng (Task 2) ✅. Không đụng copy/dedup/strip-tmp/mime (chỉ đổi sinh tiền tố + UI) ✅. Gesture/thẻ-rời giữ (`expandToScroll`, `card`) ✅. Badge/kho-changed đúng (`parseInboxPrefix.mon`, `emitKhoChanged`) ✅.
- Placeholder: không TBD; mọi step có code/lệnh. ✅
- Type: `onPick(path:string[])` khớp ChooseMonSheet↔ShareReceiver; `importSharedFile(...,path)` khớp; `makeInboxName(path[])` khớp; `SubFolder` từ types. ✅
