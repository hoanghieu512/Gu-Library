# v1.9.0 — Hiệu năng đường ghi/action (phản ánh tức thì + gom pháo)

**Goal:** Action xong là thấy ngay — từng tài liệu phản ánh khi nó xong (không đợi hết lô); lô phát 1 `khoChanged` thay N. Không đổi cái gì ghi xuống đĩa; không đụng đường đọc v1.8.0.

## Tasks
- [x] T1: `coalesceKhoChanged()` (khoEvents) — gom N emit trong lô thành 1 phát cuối; lồng nhau + lỗi-giữa-chừng an toàn (TDD, 5 test).
- [x] T2: FolderPage — batch (In/Xóa/Chuyển) bọc `coalesceKhoChanged` + phản ánh per-item (`removeDocLocal`/`flagDocLocal`); đơn (In/Xóa/Chuyển/Đổi tên) cũng per-item, bỏ `refresh()` re-read.
- [x] T3: Import có trạng thái tiến hành — `importBatch(onProgress)` + `IonLoading` "Đang nhập N/T…".
- [x] T4: Đo Drive-vs-local (`Docs/perf/2026-07-04-import-drive-vs-local.md`): local 20MB=79ms (không nghẽn); Drive = mạng trong stream (số chờ đệ đo qua overlay).
- [x] T5: Device verify Flip 4 (đo tạm rồi gỡ): xóa lô 5 → đúng 1 `khoChanged`, danh sách per-item về rỗng, kho khôi phục.
- [ ] T6: Verify 2 máy QA thật — import lô median so bảng v1.8.0 (2291/4856), đường đọc không tụt, Syncthing/worker vẫn tươi, số Drive.
- [ ] T7: CHANGELOG + memory + merge main + tag v1.9.0.

Kết quả: bổ sung vào `Docs/perf/2026-07-04-ket-qua-v1.8.0.md` (cột v1.9.0) sau khi đệ đo 2 máy.
