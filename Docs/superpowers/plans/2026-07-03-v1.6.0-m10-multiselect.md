# v1.6.0 — M10 phần 2: chọn nhiều tài liệu + action lô (In / Chuyển / Xóa) — Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans.

**Goal:** Nhấn giữ hàng tài liệu → chế độ chọn nhiều (checkbox, header "Đã chọn N", nút X); thanh đáy **In lô · Chuyển tới… · Xóa**. Action lô CƯỠI LÊN đúng logic đơn v1.5.0 (loop), không fork.

**Architecture:**
- **Kích hoạt** = long-press (timer touch ~450ms, huỷ khi move>10px hoặc nhấc sớm) → không phá tap-mở (nhấc nhanh) lẫn vuốt IonItemSliding (move ngang huỷ timer). Cờ `fired` chặn click-sau-long-press.
- **Mode state ở FolderPage:** `selectMode` + `selected: Set<pdfUri>`. Ngoài mode: hàng tài liệu = IonItemSliding (vuốt In/Xóa/⋯ v1.5.0) + long-press. Trong mode: IonItem + IonCheckbox, tap = tick/bỏ tick (không mở). Hàng THƯ MỤC: không tick, không long-press.
- **Thanh đáy IonFooter** (chỉ mode): In lô / Chuyển tới… / Xóa (đỏ-đất), disable khi 0 chọn / đang bận.
- **Action lô = loop trên hàm đơn v1.5.0:** In lô = `setPrintFlag` mỗi doc (idempotent, KHÔNG toggle); Chuyển lô = ChooseMonSheet → `moveDocument`+`moveReading` mỗi doc; Xóa lô = `deleteDocument`+`removeReading` mỗi doc. Per-doc try/catch → đếm xong/bỏ; toast kết quả; xong → thoát mode + refresh.
- **⏳ né tự nhiên:** chỉ `documents` (cặp) có checkbox; `pending` (⏳ lẻ) không → không lọt lô.
- **Back cứng thoát mode:** FolderPage đăng ký `ionBackButton` priority **60** (>50 global): trong mode → thoát mode + KHÔNG `processNextHandler`; ngoài mode → `processNextHandler()` (nhường handler 50 điều hướng).

**Bối cảnh đã xác minh:** FolderPage document row hiện = `IonItemSliding > IonItem(onClick=viewer) + IonItemOptions(In/Xóa/⋯)`; single-doc handlers `togglePrint`/`runDelete`/`doMove`/`doRename` + `setPrintFlag`(printRepo), `moveDocument`/`deleteDocument`(docRepo), `moveReading`/`removeReading`(store), `ChooseMonSheet.onPick(path,destUri)`. Back global: `useAndroidBackButton` priority 50, `ionBackButton` register API `(priority, handler(processNextHandler))`.

---

## File Structure
**Create:** `src/components/FolderDocRow.tsx` (hàng tài liệu: 2 nhánh mode + long-press).
**Modify:** `src/pages/FolderPage.tsx` (mode state + selected + thanh đáy + batch handlers + back-60 + header mode). `package.json`/`CHANGELOG`/memory.

---

## Task 1: `FolderDocRow` — long-press + 2 nhánh render

**File:** `src/components/FolderDocRow.tsx`
```tsx
import { useRef } from 'react';
import type { CSSProperties } from 'react';
import {
  IonItem, IonItemSliding, IonItemOptions, IonItemOption, IonLabel, IonIcon, IonCheckbox,
} from '@ionic/react';
import { documentTextOutline, print, printOutline, trash, ellipsisHorizontal } from 'ionicons/icons';
import type { Document } from '../storage/types';

interface Props {
  doc: Document;
  selectMode: boolean;
  selected: boolean;
  onOpen: () => void;
  onToggleSelect: () => void;
  onLongPress: () => void;
  onTogglePrint: () => void;
  onDelete: () => void;
  onActions: () => void;
}

const olive: CSSProperties = { '--background': '#4A5D3A', '--color': '#fff' } as CSSProperties;

export default function FolderDocRow({
  doc, selectMode, selected, onOpen, onToggleSelect, onLongPress,
  onTogglePrint, onDelete, onActions,
}: Props) {
  const timer = useRef<ReturnType<typeof setTimeout> | undefined>(undefined);
  const startRef = useRef<{ x: number; y: number } | null>(null);
  const fired = useRef(false);
  const clear = () => { if (timer.current) { clearTimeout(timer.current); timer.current = undefined; } };

  // Long-press CHỈ khi ngoài mode (để vào mode). Trong mode tap = toggle.
  const onTouchStart = (e: React.TouchEvent) => {
    if (selectMode) return;
    fired.current = false;
    startRef.current = { x: e.touches[0].clientX, y: e.touches[0].clientY };
    clear();
    timer.current = setTimeout(() => { fired.current = true; onLongPress(); }, 450);
  };
  const onTouchMove = (e: React.TouchEvent) => {
    if (!startRef.current) return;
    if (Math.abs(e.touches[0].clientX - startRef.current.x) > 10 || Math.abs(e.touches[0].clientY - startRef.current.y) > 10) clear();
  };
  const onTouchEnd = () => clear();

  const handleClick = () => {
    if (fired.current) { fired.current = false; return; } // nuốt click sau long-press
    if (selectMode) onToggleSelect(); else onOpen();
  };

  if (selectMode) {
    return (
      <IonItem button detail={false} onClick={handleClick}>
        <IonCheckbox slot="start" checked={selected} onIonChange={onToggleSelect} aria-label="Chọn" />
        <IonLabel className="gu-serif">{doc.name}</IonLabel>
        {doc.printFlagged && <IonIcon slot="end" icon={print} style={{ color: 'var(--gu-brown)', fontSize: 18 }} />}
      </IonItem>
    );
  }
  return (
    <IonItemSliding>
      <IonItem button detail={false} onClick={handleClick}
        onTouchStart={onTouchStart} onTouchMove={onTouchMove} onTouchEnd={onTouchEnd}>
        <IonIcon icon={documentTextOutline} slot="start" />
        <IonLabel className="gu-serif">{doc.name}</IonLabel>
        {doc.printFlagged && <IonIcon slot="end" icon={print} style={{ color: 'var(--gu-brown)', fontSize: 18 }} aria-label="Đã chọn đi in" />}
      </IonItem>
      <IonItemOptions side="end">
        <IonItemOption onClick={onTogglePrint} aria-label="Cần in"><IonIcon slot="icon-only" icon={doc.printFlagged ? print : printOutline} /></IonItemOption>
        <IonItemOption color="danger" onClick={onDelete} aria-label="Xóa"><IonIcon slot="icon-only" icon={trash} /></IonItemOption>
        <IonItemOption onClick={onActions} aria-label="Thêm" style={olive}><IonIcon slot="icon-only" icon={ellipsisHorizontal} /></IonItemOption>
      </IonItemOptions>
    </IonItemSliding>
  );
}
```
- [ ] Build. Commit `feat(v1.6): FolderDocRow — long-press + nhanh checkbox/sliding`.

---

## Task 2: FolderPage — mode + selected + thanh đáy + batch + back-60

**File:** `src/pages/FolderPage.tsx`

- [ ] **State:** `const [selectMode, setSelectMode] = useState(false); const [selected, setSelected] = useState<Set<string>>(new Set()); const [busy, setBusy] = useState(false); const [batchMove, setBatchMove] = useState(false); const [presentToast] = useIonToast();` (+ `selectModeRef` sync cho back handler).
- [ ] **Helpers:**
```tsx
const selectModeRef = useRef(false);
useEffect(() => { selectModeRef.current = selectMode; }, [selectMode]);
const exitMode = () => { setSelectMode(false); setSelected(new Set()); };
const enterModeWith = (d: Document) => { setSelectMode(true); setSelected(new Set([d.pdfUri])); };
const toggleSel = (d: Document) => setSelected((s) => { const n = new Set(s); n.has(d.pdfUri) ? n.delete(d.pdfUri) : n.add(d.pdfUri); return n; });
const selectedDocs = () => (listing?.documents ?? []).filter((d) => selected.has(d.pdfUri));
```
- [ ] **Back priority 60 (thoát mode):**
```tsx
useEffect(() => {
  const onBack = (ev: Event) => {
    (ev as CustomEvent<{ register: (p: number, h: (next: () => void) => void) => void }>).detail
      .register(60, (next) => { if (selectModeRef.current) exitMode(); else next(); });
  };
  document.addEventListener('ionBackButton', onBack);
  return () => document.removeEventListener('ionBackButton', onBack);
}, []);
```
- [ ] **Batch In (idempotent):**
```tsx
const batchPrint = async () => {
  const docs = selectedDocs(); if (!docs.length) return;
  setBusy(true);
  let ok = 0; for (const d of docs) { try { await setPrintFlag(d.pdfUri); ok += 1; } catch { /* skip */ } }
  setBusy(false); exitMode(); refresh();
  await presentToast({ message: `Đã đánh dấu ${ok} tài liệu cần in`, duration: 2000 });
};
```
(import `setPrintFlag` từ printRepo — đã có `clearPrintFlag` import.)
- [ ] **Batch Delete:**
```tsx
const batchDelete = () => {
  const docs = selectedDocs(); if (!docs.length) return;
  presentAlert({
    header: 'Xóa tài liệu?',
    message: `Xóa hẳn ${docs.length} tài liệu khỏi kho (mọi máy sau khi đồng bộ). Không hoàn tác.`,
    buttons: [{ text: 'Hủy', role: 'cancel' }, { text: 'Xóa', role: 'destructive', handler: () => { void runBatchDelete(docs); } }],
  });
};
const runBatchDelete = async (docs: Document[]) => {
  setBusy(true);
  const root = await getRootUri();
  let ok = 0, fail = 0;
  for (const d of docs) {
    try {
      const rel = root ? relPathFromUris(root, d.pdfUri) : null;
      await deleteDocument(decoded, baseOf(d));
      if (rel) await removeReading(rel);
      ok += 1;
    } catch { fail += 1; }
  }
  setBusy(false); exitMode(); refresh();
  await presentToast({ message: fail ? `Xóa ${ok}, lỗi ${fail}` : `Đã xóa ${ok} tài liệu`, duration: 2500, color: fail ? 'danger' : undefined });
};
```
- [ ] **Batch Move** (ChooseMonSheet dùng chung — mở khi `batchMove`):
```tsx
const batchMoveDo = async (_path: string[], destUri: string) => {
  const docs = selectedDocs(); setBatchMove(false);
  if (!docs.length || !destUri) { exitMode(); return; }
  setBusy(true);
  const root = await getRootUri();
  let ok = 0, fail = 0;
  for (const d of docs) {
    try {
      const oldRel = root ? relPathFromUris(root, d.pdfUri) : null;
      const newBase = await moveDocument(decoded, baseOf(d), destUri);
      const destRel = root ? relPathFromUris(root, destUri) : null;
      if (oldRel && destRel != null) await moveReading(oldRel, `${destRel}/${newBase}.pdf`, d.name);
      ok += 1;
    } catch { fail += 1; }
  }
  setBusy(false); exitMode(); refresh();
  await presentToast({ message: fail ? `Chuyển ${ok}, lỗi ${fail}` : `Đã chuyển ${ok} tài liệu`, duration: 2500, color: fail ? 'danger' : undefined });
};
```
- [ ] **Header mode + rows + footer + move sheet:**
  - Toolbar: `selectMode ? (X button onClick=exitMode + title "Đã chọn N") : (back + "Môn / Chương" + nút tạo)`.
  - Document rows → `<FolderDocRow doc selectMode selected={selected.has(d.pdfUri)} onOpen=viewer onToggleSelect=()=>toggleSel(d) onLongPress=()=>enterModeWith(d) onTogglePrint=()=>togglePrint(d) onDelete=()=>confirmDelete(d) onActions=()=>setActionsDoc(d) />`.
  - Folder rows: giữ nguyên (không long-press, không tick).
  - Footer (chỉ selectMode): 3 nút In lô / Chuyển tới… (setBatchMove(true)) / Xóa (batchDelete), disabled `selected.size===0 || busy`.
  - `<ChooseMonSheet isOpen={batchMove} note={`Chuyển ${selected.size} tài liệu`} onPick={batchMoveDo} onCancel={()=>setBatchMove(false)} />` (thêm cạnh sheet move đơn hiện có).
- [ ] Build + test. Commit `feat(v1.6): chon nhieu + In/Chuyen/Xoa lo (cuoi len logic don) + back thoat mode`.

---

## Task 3: Build APK + verify 2 máy QA
- [ ] Build release; cài Z Flip 4 + máy QA 2.
- [ ] Checklist (brief): long-press vào mode (hàng đầu tự tick); tap tick/bỏ; folder không tick; thoát bằng X + back cứng; In lô 5 (2 có sẵn giữ nguyên); Chuyển lô 6 drill 2 cấp + 1 trùng tên `(k)`; Xóa lô nêu số lượng → sạch máy 2; ⏳ không lọt lô; ngoài mode tap-mở + vuốt v1.5.0 không hồi quy; "Phiên bản 1.6.0".

## Task 4: bump 1.6.0 + CHANGELOG + memory + tag + push.

---

## Self-Review
- Long-press vào mode + không phá tap/swipe (Task 1 timer+move-cancel+fired) ✅; checkbox tap toggle, folder không tick (Task1/2) ✅; thanh đáy In·Chuyển·Xóa (Task2) ✅; In lô idempotent `setPrintFlag` (không toggle) ✅; Chuyển lô một đích ChooseMonSheet dùng chung ✅; Xóa lô nêu số lượng ✅; ⏳ né (chỉ documents có checkbox) ✅; back thoát mode (priority 60) ✅; đổi-tên KHÔNG có ở lô ✅; ride logic đơn (loop `setPrintFlag`/`moveDocument`+`moveReading`/`deleteDocument`+`removeReading`, không fork) ✅; per-doc try/catch báo xong/lỗi ✅; token sẵn (checkbox default, Xóa danger, olive ⋯) ✅.
- Type: `FolderDocRow` props khớp FolderPage callbacks; `selectedDocs()` từ listing.documents; `batchMoveDo(path,destUri)` khớp ChooseMonSheet.onPick. ✅
